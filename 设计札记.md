# 设计札记

4.0 在不动地基的前提下加了一层“读者引擎”。地基（候选、摘要批准、可恢复事务、备份、永久 ID、账本、封段、doctor、repeat 与 style）保持原样；上层把规划单位换成弧、生成单位换成三章一批，并让“读者为什么点下一章”排在每一份读取包的最前面。事故与规则的对应见规则由来 R84—R91。

3.15.2 只修正证据与措辞不一致：没有找到来源不等于已证明错误，输出末页不等于已经输出或读完前页。沿用原诊断清单和分页参数，不新增表格、阅读会话或质量门槛。

这份文件只供母版维护者阅读，不进入新书项目。

历史事故与现行规则的逐条对应见 `规则由来.md`。这里保留架构说明，复盘证据单独维护，避免入口文档重新膨胀。

3.15.1 的精简遵循两个边界：同一次只读任务中的材料和判断可以复用；跨正式写入时点的基线与批准必须重新核对。完整候选集合仍是批准对象，实际写入集合只含字节变化的文件及因账本变化需要刷新时间的图谱。规则从原来源维护，不为轻量接续再建立第二套大纲、账本或质量批准流程。

## 系统要解决的问题

长篇小说会不断增加人物、关系、物件、规则、承诺与章节。模型和作者都无法长期依靠记忆保持这些内容一致。系统因此把已经确认的故事状态写进可追踪文件，并在每章只组装当前需要的材料。

目标包括以下几项。

- 已经成立的事实有明确来源
- 此刻状态与历史记录分开
- 内部引用不受删章、并章和移动顺序影响
- 多份相关记录一起提交
- 输入缺失会停止流程
- 题材规则可以增加，核心文件保持稳定

机器检查的范围限于结构、状态、引用、文件和事务。读者引擎卡、弧卡和悬念账用于记录创作约定；钩子与回报由作者审阅，工具核对记录。结构检查通过不证明文学效果。

## 文件职责

`00_设定层` 保存低频变化的故事约定。

`01_运行层` 保存当前状态、活动伏笔和近期事实。

`02_检查层` 保存逐章检查、全书改稿与执行契约。

`03_读者层` 保存真实阅读反馈。

`04_题材插件` 保存可选扩展及其安装包。

`05_正文` 只保存作品正文。

`06_归档` 保存章节卡、候选走向、流程审计与插件历史。

`.novel` 保存事务日志、提交前后字节和运行状态，不进入读取包与备份。

## 唯一信息来源

`project.json` 决定项目结构版本、插件选择与归档基线。

`system-manifest.json` 决定母版必需文件、项目必需文件与共享同步文件。

插件自己的 `plugin.json` 决定安装文件和核验标记。

`_工具/排除表.py` 决定哪些运行目录不参与扫描、影子验证和备份。

这些清单不在说明文档中复制。修改清单后必须运行发布自测。

## 多文件提交

章节变化先写入 `_候选/KNNNN`。系统在影子项目中运行图谱和提交闸门。影子通过后，正式文件由可恢复事务逐份原子替换。全部替换完成后再做正式复核。

事务日志先写，正式文件后写。普通异常尝试恢复；输入不完整或恢复失败时保留日志并明确报错。**进程被强制终止后恢复不会自己发生**：只读命令报告存在未完成事务，写入命令拒绝继续，直到用户显式运行 `recover`——恢复会改写正式文件，它和提交一样是一次写入。`recover` 按日志把文件退回事务开始前，或补完一笔已经通过验证并进入提交目录的事务。

最近一次成功事务可以回退。回退本身也形成新事务，原历史仍然保留。

## 插件边界

核心流程不假定小说属于某一题材。插件只能在自己的设定、运行、检查和归档路径增加文件。

插件安装遵守以下约束。

- 清单声明所有目标文件
- 目标已有不同内容时停止覆盖
- 安装与卸载使用多文件事务
- 插件数据存在时，普通卸载会拒绝
- 强制卸载先把原文件移入事务历史目录
- 读取包只装入已经启用的插件

新题材先尝试无插件核心。只有一组规则在多章中反复需要时，才值得做成插件。

## 读取预算

简报用于准备章节卡，不读取章节卡与定向旧章。

正式读取包包含执行契约、核心检查、章节卡、当前状态、近期正文与显式调阅的旧章。插件启用后再加入插件设定、运行表和检查文件。

读取包不静默截断。超过目标会报告主要来源，超过硬上限会停止生成。

## 备份

阶段备份使用压缩包和逐文件摘要。候选区、读取包、旧快照、旧备份、版本库与事务历史不会再次进入备份。

恢复只能写入尚不存在的新目录。现有项目不会被恢复操作覆盖。

## 读取包的带宽经济

每章能给模型的字符数是固定的。读取包的全部设计围绕一件事：把这笔预算花在**这一章真正需要的材料**上。

预算的敌人有三个，各自的对策不同。

1. **只增不减的账本**。`06_事实记录.md` 按窗口读取，较早条目只带预览与长期保留项；历史预览仍会累计。对策是封段：把已经压成阶段摘要的旧段搬进 `06b`，`06` 只留摘要。这是一条承重流程，所以它必须是命令，不能只是模板注释里的一句话，使读取压力超过预算前有明确的整理入口。
2. **整章调阅**。章节卡不给片段锚点就整章进包。首尾锚点必须逐字唯一，取不到就停，不猜。
3. **悄悄膨胀而没人看见**。所以带宽预警要在**远未撞线时**就出现：事实记录过目标线三成、成品包到硬上限八成五。撞线才报错等于不报。

`status` 把这三件事放在同一屏，因为它们只有放在一起看才知道该先动哪一个。

## 一条检查值不值得信

这套系统里最危险的东西不是漏检，是**看起来在检查、实际上恒真的检查**。它和真的通过长得一模一样，而且会一直绿下去。

已经出现过两次，成因相同：

- v1 的审计示例假通过——检查跑在示例数据上，永远有结果
- v2 的状态快照 C／D／E 空转——排除表头用名单匹配，模板改名后名单失效，表头行自己顶成"有内容"

两次都不是逻辑写错，是**判据依赖了一个会漂移的外部约定**，而样本又刚好没有咬住那个约定。v2 的读取包有 32 条对抗样本全绿，因为它们全用自造夹具，而夹具的表头停在 v1 的名字，测的是一份已经不存在的模板。

所以现在有两条硬约定。

**第一，凡是拿名单匹配模板的检查，改成按结构取。** 表头是"表格的第一行"，不是"名字在这几个里面的行"。结构不会因为改名而漂移。

**第二，凡是判据依赖模板约定的检查，必须有一条直接读现行模板的样本。** 不是夹具，是母版里那份文件本身。空模板必须全报，填好必须一条不报。夹具用来测边界，真模板用来防漂移，两者不能互相替代。

判断一条新检查值不值得留，问一句：**它在什么输入下会失败？** 造不出那个输入，这条检查就还没有被验证过。


## 文风层放在哪一层

去 AI 感有三个可能的下手位置，价值差别很大。

**生成之前**是主战场。跨平台写作时，聊天里说的"别写得像 AI"下一段就失效——上下文一滚就没了。读取包是唯一每章都会重新出现的通道，所以 `11_文风基线.md` 必须进包，而且必须是 CONTROL。这一条决定了这个功能能不能管住 Claude Code 以外的平台。

**生成之后**是补漏。机器定位命中行号，人拿着行号定点改。注意顺序：先跑机器再人工通读，反过来会被 AI 腔的顺滑骗过去——它读起来是通的，问题在于它不属于这本书。

**提交闸门**不做。理由见 规则由来 R57。

还有一个位置容易被忽略：**由三章/十二章复盘承接的连续并排检查**。单章都在容差内、但连续十章朝同一方向漂，是最难自己发现的失败模式。每一章都说得过去，合起来整本书变味了。

## 修改母版

每次修改应按下面顺序收口。

1. 更新实现与唯一清单
2. 更新直接受影响的说明
3. 建立一个必然失败的故障样本。判据依赖模板约定时，样本必须读现行模板本身
4. 把版本号之类的常量从内核读，不要在测试里写死——写死的常量会在下一次改版时把样本变成恒真
5. 运行无插件与四插件测试
6. 运行事务中断、备份和迁移测试
7. 用至少一个已有长项目做兼容回归
8. 清理测试产物与项目专名

一条检查如果不能稳定区分正确与错误，应先降为提醒。持续误报的检查应删除或移入人工复读，不应靠不断增加豁免维持。

## 读者引擎层放在哪一层

引擎卡是 DATA，不是 CONTROL。它是作者和代理一起写下的创作约定，可能含有任何文字；按“要不要照着做”划分信任边界（R47），它和章节卡同类。它排在包的最前面，是为了让模型先读“读者冲着什么来”，再读“什么是真的”。

启用判据只有一条：引擎卡的核心栏已经填写。这样旧项目同步后多出来的空模板不会改变任何行为；新书在开书 D11 就填好，从第一章起启用。钩子检查再收窄到“作者批准的弧卡范围内”：它核对的是批准的章节拍有没有兑现，没有弧卡就没有可核对的计划，这时只提醒、不拦。

一条新检查值不值得留，仍然问：它在什么输入下会失败？四点零引擎组为每一条新检查都造了一个必然失败的输入——缺钩子行、非弧末用弧末收束、类型不在七类、快照没到批末、批次超上限、引擎卡是模板。

规则膨胀要有人守。R91 在新增规则前多问一句“它会不会让故事更平”，并给读取包里的 CONTROL 定了 5,000 字符的发布上限；自测直接量成品包。

## 文风档放在哪一层

文风档是 CONTROL：它规定生成时怎么写、第二遍怎么查，和 08 检查提示同类，而且内容来自母版、不是作者粘贴的资料。本书的正样本仍是 DATA，是这本书自己的声音；档给默认，样本给个性，两者都进包。

它和题材插件分开，是因为“写什么”和“怎么讲”不一定绑在一起：网文也有人想写得冷，悬疑也有人想写得爽。插件只推荐默认档。

去 AI 腔从“全部拦”改成“按档拦”，没有削弱检查：母版词表里本来就写着网文会天然命中、应当豁免，旧版依赖逐项目填写豁免表。现在把豁免做成档的默认，本项目仍可以再收紧。
