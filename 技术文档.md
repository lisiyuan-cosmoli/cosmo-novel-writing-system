# 技术文档 · v6.0

> 这一份写给**要动母版的人或代理**：改代码、改模板、加检查、加插件、发下一个版本。
>
> 写小说不需要读它。写小说读 `START_HERE.md`（代理）或 `使用说明_给你自己看.md`（作者）。
>
> 软件维护专用，不进读取包；原地开始会保留本文，旧 `init` 另建目录时排除。

## 为什么单独有这一份

3.6.0 交付时带着一处会阻断第二章的断裂：模板把 `### 取样范围` 删了，`_工具/章节卡.py` 还在按这个标题取值，而两份测试夹具停在旧结构上恰好绕开了它——215 项全绿，第二章走不通。

改模板的人不知道有段代码依赖那个标题。这份文档就是那张缺失的地图。

它和其他文档的分工：

| 文档 | 回答什么 | 读者 |
|---|---|---|
| `README.md` | 这是什么，能做什么 | 所有人 |
| `使用说明_给你自己看.md` | 我该怎么用 | 作者 |
| `START_HERE.md` | 这一章按什么顺序做 | 执行代理 |
| `02_检查层/代理执行协议.md` | 我能碰什么、不能碰什么 | 执行代理（**信任边界唯一准本**）|
| `设计札记.md` | 为什么是这个结构 | 维护者 |
| `规则由来.md` | 这条规则是哪次事故来的 | 维护者，查规则时 |
| `已知限制.md` | 机器证明不了什么 | 所有人 |
| `CHANGELOG.md` | 每版改了什么 | 维护者 |
| `交付验收报告.md` | 这一版验到什么程度 | 验收者 |
| **`技术文档.md`（本文）** | **代码怎么组织、契约在哪、改之前要核什么** | **维护者，动手前** |

本文不复制任何清单的内容——清单以机器可读的那一份为准（见 §四）。它只说清单在哪、谁读它、改了之后什么会失效。

## 一、分层与数据流

目录层次是**读取频率**的分层，不是题材分类：

```
00_设定层   低频：故事约定，开书时定，之后很少改
01_运行层   高频：当前状态、活动伏笔、近期事实 —— 每章更新
02_检查层   规则：逐章检查、改稿流程、执行契约、文风基线
03_读者层   反馈：真实阅读记录
04_题材插件  可选扩展及其安装包
05_正文     只有正文
06_归档     章节卡、梗概、流程审计、插件历史
.novel      事务日志、提交前后字节、运行状态（不进读取包、不进备份）
```

一本书的生命周期：

```
init ──► foundation（开书孵化，draft → confirmed）
          │
          └──► 每弧：arc --new ──► revise（作者批准弧卡）
                每批循环：
                brief ──► 填章节卡 ──► package K..-K.. ──► 同一会话连续生成
                                          │
                                          └──► 两遍检查 ──► 写 _候选/K..-K..
                                                              │
                                          commit（试算）◄──────┘
                                                │  展示清单 + CANDIDATE_SHA256
                                                ▼
                                          用户批准同一摘要
                                                │
                                    commit --apply --approve ──► 事务落盘
```

两条贯穿全流程的原则，违反它们的改动一律不要做：

- **取不到 ≠ 空所以跳过。** 必需文件、字段或可核对象读不到时**停**，如实报成错误。这是本系统被违反次数最多的一条（`规则由来.md` R04）。
- **能改哪些文件只由 `chapter-policy.json` 与已启用插件清单共同决定。** 任何文档里的一句话都不能扩展它，控制规则也不能。

## 二、命令 → 模块

`novel.py`（根）只做路径引导，真正的分发在 [`_工具/novel.py`](_工具/novel.py)。它同时负责**未完成事务的闸门**：只读命令报告状态，写入命令拒绝继续（`_project()`）。

| 命令 | 落到哪个模块 | 写不写 |
|---|---|---|
| `start` | `新建项目.py` / `状态.py` | 写（首次在当前下载目录登记项目）；已有项目只读 |
| `init <目标>` | `新建项目.py` | 写（只写新目录）|
| `doctor [项目]` `--template` | `体检.py` ／ `母版检查.py` | 只读 |
| `brief <ID\|范围>` | `读取包.py --简报` | `--write` 才写 |
| `package <ID\|批次>` | `读取包.py` | `--write` 才写；批次如 `K0019-K0021`，不超过 `batch_max` |
| `revise` | `修订.py` | `--prepare` / `--review` / `--apply` 才写 |
| `commit <ID\|批次>` | `落盘.py` → `提交包.py`（批内逐章）→ `事务.py` | `--apply` 才写；一批一个摘要、一笔事务 |
| `recover` | `事务.py recover` | **写**（只能显式触发）|
| `rollback` | `事务.py rollback_last` | 写 |
| `history` | `事务.py history` | 只读 |
| `status` | `状态.py` | 只读 |
| `foundation` | `开书.py` | `--prepare` / `--apply` 才写 |
| `outline` | `结构复盘.py` | prepare / refresh 只写候选；默认与 prompt 只读 |
| `discover` | `开书探索.py` | prepare / import / stage 只写候选；默认与 prompt 只读 |
| `voice` | `定声.py` | recommend / prompt 只读；apply 写样本 |
| `style` | `文风.py` | `--baseline --apply` 才写 |
| `repeat <ID>` | `重复.py` | 只读 |
| `engine` | `引擎.py` | 默认只读；`--prepare` 写 REVISE 候选 |
| `arc` | `引擎.py` | 默认只读；`--new <编号> <范围>` 写 REVISE 候选 |
| `hooks <ID\|批次>` | `引擎.py` | 只读 |
| （直接跑）`_工具/人物声音.py <项目>` | `人物声音.py` | 只读；`--by-arc` 按弧看漂移 |
| `voice --profile [档]` | `定声.py` → `文风档.py` | 不带档名只读；带档名写 project.json（事务） |
| `guide [插件]` | `题材手册.py` | 只读 |
| `seal <起> <止>` | `封段.py` | `--apply` 才写 |
| `backup` / `restore` | `备份.py` | 写 |
| `migrate <项目>` | `迁移项目.py` | `--apply` 才写 |
| `sync <项目>` | `同步文件.py` | 写（母版专用）|
| `plugin ...` | `插件.py` | install/uninstall 写 |
| `release-test` | `发布自测.py` | 只写临时沙盘（母版专用）|

**只读命令在任何异常状态下都不改动项目里的任何字节**——这条有专门的测试组（只读组）逐文件比对 SHA256。加新命令时，如果它只读，必须走 `_project(..., 写入=False)`。

## 三、工具模块

Python 文件按职责分四类，实际名单以 `_工具/*.py` 为准。改动前先看它属于哪一类。

**内核与事务**

| 文件 | 职责 |
|---|---|
| `v2_core.py` | 公共内核：项目解析、读写、母版判定、插件常量 |
| `事务.py` | 崩溃可恢复的多文件事务。`flock` 互斥、日志先写、逐份原子替换 |
| `排除表.py` | 哪些运行目录不参与扫描、影子验证与备份。**唯一来源** |
| `模块表.py` | 插件 ID → 运行表／设定／检查文件的映射。加插件只改这一张表 |

**每章主链**

| 文件 | 职责 |
|---|---|
| `读取包.py` | 只测量、只装配、超线即停。**一个字都不生成** |
| `章节卡.py` | 章节卡结构校验（`package` 与 `commit` 两个阶段共用）|
| `落盘.py` | 候选提交与正式写入闸门 |
| `提交包.py` | 按项目条件启用的提交闸门实现（见 §六）；批次提交时由落盘逐章调用，`--批末` 指定快照口径 |
| `文风档.py` | 4.1 题材文风档：登记表、当前档、去 AI 腔放行类别、表现力度量与“往克制漂”提醒；4.1.2 情绪词重复提醒；`voice --profile` |
| `引擎.py` | 4.0 读者引擎层：引擎卡、弧卡、悬念账与批次范围的解析；`engine`、`arc`、`hooks` 命令 |
| `图谱_解析.py` / `生成图谱.py` | 把核心账本解析成题材中立的图谱数据 |
| `重复.py` | 重复动作检索 + 章末不可逆变化并排对比 |
| `公平性.py` | 悬疑插件专属：揭晓之前这条线索有没有真的铺过 |

**开书、文风与维护**

| 文件 | 职责 |
|---|---|
| `修订.py` | 全局创作修订；完整基线摘要、差异预览、影子体检与事务回退 |
| `开书.py` | 开书孵化，与章节提交同一套闸门（候选区／试算／摘要批准）|
| `结构复盘.py` | 原大纲论证、阶段复盘候选与资料版本提醒 |
| `开书探索.py` | 可选研究、原始材料副本及现有候选衔接，不联网 |
| `定声.py` | 文风共创：把"这本书是什么声音"变成一项要确认的决定 |
| `文风.py` | 文风度量：指纹层、条款腔（4.1.1）、分布层，以及定点改写提示词 |
| `状态.py` | 一屏项目状态 |
| `体检.py` | 题材中立的项目体检 |
| `封段.py` | 事实记录封段，只搬运，不代写摘要 |
| `备份.py` | 压缩备份与只读校验。符号链接默认拒绝 |
| `插件.py` | 声明式插件安装／核验／卸载 |
| `题材手册.py` | 调阅题材手册（不进读取包）|

**母版专用**（一份都不进新项目）

| 文件 | 职责 |
|---|---|
| `优化自测.py` | 创作意图、全局修订、路线沿用与历史事实保留的端到端回归 |
| `读取效率自测.py` | 片段覆盖去重、锚点拒绝、历史事实与正式包回归 |
| `运行效率自测.py` | 同次读取复用、文风来源检查、目录索引与只读边界 |
| `修订效率自测.py` | 实际写入集合、完整批准绑定及原子替换权限保留 |
| `发布自测.py` | 发布级回归，报告项数以本版实跑输出为准 |
| `数据保护自测.py` | 6.0 候选保留、备份一致性、插件并发及多次回退反例 |
| `工作流自测.py` | 6.0 前章缺失、批次候选、调序简报与文风候选衔接 |
| `创作模式自测.py` | 6.0 通用／升级模式、现行模板和旧书兼容 |
| `新书自测.py` | 新书全流程：建目录跑到 K0001 正式包 |
| `读取包自测.py` | 读取包对抗样本集，持久化的必报警清单 |
| `同步自测.py` | 同步工具回归 |
| `新建项目.py` | 原地登记下载目录，或从母版另建项目，并维护**母版专用清单** |
| `同步文件.py` | 按 `system-manifest.json` 同步母版拥有的共享文件 |
| `迁移项目.py` | 旧项目迁到当前结构 |
| `母版检查.py` | 母版结构、发布清单与项目数据残留 |

依赖方向是单向的：主链模块依赖内核，内核不依赖主链；自测依赖被测模块，被测模块不知道自测存在。**不要反向 import**。

## 四、唯一信息来源

清单只有一份，机器可读的那一份说了算。**不要在文档里复制它们的内容。**

| 文件 | 决定什么 | 谁读它 |
|---|---|---|
| `project.json` | 结构版本、插件选择、开书状态、归档基线 | 几乎所有命令 |
| `system-manifest.json` | 母版必需文件、项目必需文件、共享同步文件、种子文件、合并区块 | `同步文件.py`、`母版检查.py`、`新建项目.py` |
| `chapter-policy.json` | **章节提交能改哪些文件** | `落盘.py`、`提交包.py` |
| `revision-policy.json` | 全局创作修订的可写范围 | `修订.py` |
| `foundation-policy.json` | 开书事务能改哪些文件 | `开书.py` |
| `04_题材插件/_包/<题材>/plugin.json` | 该插件安装哪些文件、核验哪些标记、声明哪些逐章可变文件 | `插件.py`、`读取包.py` |
| `_工具/排除表.py` | 哪些目录不参与扫描 | 影子验证、备份、体检 |

`project.json` 字段示例（合成内容）：

```json
{ "format": "novel-project", "schema_version": 2, "system_version": "3.12.0",
  "project_id": "…", "title": "示例小说",
  "plugin_decision": "none", "enabled_plugins": [],
  "initialization": { "status": "confirmed", "mode": "guided",
                      "approved_sha256": "…", "confirmed_at": "…" },
  "archive_required_from": "K0001", "time_model": "free_text",
  "created_at": "…", "updated_at": "…" }
```

`initialization.status` 三态：`draft`（写不了第一章）／`confirmed`（只能由 INIT 正式事务写入）／`legacy`（旧项目升级来的，可以继续写）。

`system-manifest.json` 的 `previous_sync_baselines` 保存已核实旧版共享文件的逐文件哈希，用于没有安装基线的旧项目。当前源文件哈希由 init/sync 实时计算，并登记在项目的 `system_file_hashes`。升级时保留历史基线原值，按实际旧母版快照补充基线；不能把改后的当前哈希填回旧版本，也不能只更新版本文字和文件名单。

改任何清单之后必须跑 `release-test`。

## 五、事务模型

```
.novel/
  transaction.lock      flock 互斥，文件常驻不删（删了会绕过互斥）
  HEAD                  当前生效的事务 ID
  commits/<事务ID>/
      manifest.json     id parent phase files applied after_sha256
                        missing_before label metadata pid committed_at …
      before/           替换前的字节
      after/            替换后的字节
  transactions/         事务运行期日志
  runtime/              运行状态
```

阶段推进：`prepared → applying → validating → commit_ready → committed`。回退写 `rolled_back`，恢复写 `recovered`。

**恢复语义（v3.1 起，别写回 v3.0 的说法）**：进程被强制终止后恢复**不会自己发生**。只读命令报告"存在未完成的事务"，写入命令拒绝继续，直到用户显式运行 `novel.py recover`。恢复会改写正式文件，它和提交一样是一次写入。`recover` 按日志把文件退回事务开始前，或补完一笔已经进入 `commit_ready` 的事务。

`rollback` 撤销最近一笔提交，**它自己也是一笔提交**——连按两次是重做，不是再退一章。

互斥的可靠范围是**同一台机器上的本地文件系统**。iCloud／Dropbox／OneDrive／网络盘上 `flock` 行为不保证，取不到锁时工具明确报错，不假装加上了。

批准摘要在**锁内重算**：试算给出的 `CANDIDATE_SHA256` 只在候选内容、文件清单、路径和正式基线都没变时有效，否则整笔拒绝。这挡的是"旧批准覆盖新提交"。

## 六、闸门

分两个阶段，两个阶段都调 `章节卡.校验`（`phase='package'` / `phase='commit'`）。

**package 阶段**核对开写所需内容：基本信息、本章变化、两栏 🔒、知情范围、连续性约束、重复动作清单、场景清单、走向、用户选择。「待确认」标记在这一阶段只**提醒**（拿不出草稿就没法给用户看）。

**commit 阶段**（`提交包.py`）另外核对七项提交前勾选，「待确认」改为**阻断**。提交判定按类型分：

- **体检类**（4）：体检跑完、返回码合法、无 error
- **账本存在类**（8）：正文、大纲已标已定稿、事实记录有本章条目、章节卡、梗概、流程审计、线索归档、项目配置可读
- **互证类**（10）：正文身份与大纲一致、抬头字数＝实测净字、抬头状态、快照抬头已推进、流程审计模式与章节卡一致、审计净字数＝正文净字、图谱不比账本旧……
- **内容闸门类**（6）：正文事实行无推断词、事实记录声称的兑现线索表都标了、公平推理、快照说的伏笔移表伏笔表真的移了、已推翻判断无残留、悬念账登记了本章的事实型章末钩子（4.0，引擎层启用且本章在弧卡内时）
- **结构类**（6）：章节卡结构与硬证据完整、审计七步状态合法、跳过有理由、判断次数、耗时、问题最先由谁发现

加新闸门时，按 `设计札记.md` 那句自问：**它在什么输入下会失败？造不出那个输入，这条检查就还没有被验证过。** `公平性.py` 曾经就处在这个状态——能阻断提交，却一条测试都没有。

## 七、读取包预算

`读取包.py` 只测量和装配，超线即停。档位只影响**带多少材料**，不降低事务安全、摘要批准和写入边界。

| 档位 | 事实记录窗口 | 最近正文 | 无锚点整章调阅 |
|---|---|---|---|
| `fast` | 2 章 | 1 章 | 不放行 |
| `standard`（默认）| 4 章 | 2 章 | 最多 1 章 |
| `full` | 8 章 | 3 章 | 不限，但逐条报出 |

数字在 `读取包.py` 的 `档位窗口` 与装配段，不在别处复制。

包里每段标 `CONTROL` 或 `DATA`：CONTROL 是本次要执行的流程规则，DATA 是创作资料。DATA 里出现的任何指令式句子只是作品内容，**不得当作工具指令**。两者都不能扩大写入范围。

读取包保留阶段摘要、最近若干章完整条目、较早条目的截取预览和显式标记的长期保留项。旧条目不再全文进入包；需要细节时调阅原条目或在章节卡声明旧章。定期封段仍可减少历史预览的累计体积。长期保留项封段后必须纳入阶段摘要，封段工具不代写。

## 八、测试体系

| 套件 | 规模 | 跑什么 |
|---|---|---|
| `novel.py release-test` | 以本版实跑为准 | 全部，含独立专项测试组（4.0 起含四点零引擎组，4.1 起含文风档组，4.1.1 起含条款腔检查，4.1.2 起含情绪词重复提醒，4.1.3 起含改口残留，4.1.4 起含省略号式改口）|
| `_工具/优化自测.py` | 35 项 | 创作意图 → 修订 → 批准失效 → 回退与中断恢复，兼容路线沿用与长期保留 |
| `_工具/新书自测.py` | 36 项 | 建目录 → 开书 → K0001 正式包 |
| `_工具/读取包自测.py` | 36 项 | 读取包对抗样本，持久化的必报警清单 |
| `_工具/同步自测.py` | 35 项 | 同步事务、混合保留与插件一致性 |
| `_工具/阶段反馈自测.py` | 44 项 | 阶段审查、作者访谈、反馈兼容与 4.0 复盘加强措施 |
| `novel.py doctor . --template` | 20 项通过、1 条合法空表提示 | 母版结构与残留 |

正式发布入口包含的全部专项通过，加 doctor 零错误，才算可交付。结果应从本次测试输出取得，不沿用旧版统计。

**两条硬约定**（`设计札记.md`，违反过三次，第三次就是 3.6.0）：

1. **凡是拿名单匹配模板的检查，改成按结构取。** 表头是"表格的第一行"，不是"名字在这几个里面的行"。结构不会因为改名而漂移。
2. **凡是判据依赖模板约定的检查，必须有一条直接读母版现行模板的样本。** 不是夹具，是母版里那份文件本身。空模板必须全报，填好必须一条不报。夹具用来测边界，真模板用来防漂移，两者不能互相替代。

对应的两组测试：

- **现行模板端到端组**（19 项）：只用母版 `01_运行层/07_章节卡_模板.md`（4.0 瘦身卡），真跑 `repeat --card` → 贴 → 校验 → `package`，覆盖第一至第三章、空模板全报与结构不变量；3.15 旧卡、3.5 旧卡与 3.6.0 过渡卡的兼容样本由 3.15.2 旧模板原文生成
- **四点零引擎组**（23 项）：引擎卡与 D11、弧卡、批次包排序与 CONTROL 上限、批内取样、钩子与快照逐项拦截、整批提交与回退
- **文风档组**（10 项）：四档登记、选档事务、网文档进包、去 AI 腔按档放行与照拦、往克制漂提醒、未选档旧项目兼容
- **退役文案组**（1 项）：按**文案本身**在全库查已废弃的规则原文，只豁免职责就是记录旧说法的四份文档。一句话废弃了，副本散在别处继续说着旧规则——这是 3.6.0 教训的第二层

加测试的位置：在 `发布自测.py` 里写一个 `xxx组(sandbox)` 函数，用 `record(名, 条件, 详情)` 登记，然后在 `main()` 里调用。`record` 的 detail 会被转成字符串，传列表也不会炸报告。

## 九、改母版的规程

### 先核对部署母版与维护副本

部署母版保持唯一；隔离副本用于开发和测试，历史版本用于比对与回退。先核对真实目录、版本、Git 状态与文件指纹，不能把当前工作树误当成 HEAD，也不能从旧报告推断实时状态。原有版本控制资料应保留。

1. 保存原外层目录的完整可恢复备份与逐文件指纹，包括 Git 和脚本；母版内不运行项目 backup，也不初始化 project.json。
2. 在母版之外建立隔离副本，先用当前模板复现故障；将模板、字段、标题和路径依赖追到解析器、生成器、校验器与旧数据。
3. 在副本修复并将反例纳入现有自测；复用当前候选、事务、清单与插件机制，不重复建立流程。
4. 用当前模板实际建书，按改动范围覆盖读取包、提交、修订、同步或脚本入口；正式发布运行 release-test 和 doctor --template。
5. 同步核对文档、版本、清单、安装基线、展示页及受影响的外层脚本。各项结果从本轮输出填写，子测试汇总不与发布总数重复相加。
6. 验证通过后在已有授权范围部署；部署前重新比对源目录，出现其他并行改动先处理差异。部署后核验真实文件、母版体检和纯净性。
7. 原始输出、问题过程、合成数据及回退资料保存在独立维护工作区；母版只接收通用代码、规则、空白模板和必要说明。

作品迁移与系统部署分开。展示具体项目、sync --check 差异、定制内容处理和备份方案，在获得该范围授权后再同步；已有授权不重复询问。同步后核验正文、设定、大纲、作者记录、候选与定制插件，重新体检。系统升级不构成正文改稿批准。

新项目根目录多一份文件时，记得同时加进 `新建项目.py` 的 `母版专用` 集合（如果它只属于母版）和 `system-manifest.json` 的 `template_required_files`。

## 十、这份文档里哪些内容是机器核过的

文档会腐烂，这个项目栽过三次。所以本文的两处清单由发布自测核对：

- §二 的命令表必须列全 `novel.py` 的所有子命令
- §三 的模块表必须列全 `_工具/*.py`

漏一个就报错，并指出漏的是哪个。剩下的内容（数据流、事务语义、闸门分类、改动规程）机器核不了，靠改动时一起改——这一条限制如实写在这里，不假装全文都被验证过。

自测只能验证指定目录，不能证明外部部署位置、授权范围或其他副本的状态。部署母版与隔离副本的身份由维护记录、路径核对和部署前后指纹共同追踪。

## 十一、机器证明不了什么

工具能证明文件存在、字段非空、状态互证、摘要一致、事务结果。

工具**不能**证明用户亲自提供了某段内容、真正读完了正文、偏好某个走向，或某一章有文学质量。开书闸门同理：它能证明十一项决定被逐条提出过、有明确来源、批准时全部已确认，**不能证明批准者的现实身份，也不能证明用户读过每个候选**。

不要把这些复述成"用户参与已验证"。完整清单见 `已知限制.md`。

## 3.8 修订关联核对与章节重心

`v2_core.body_header_errors` 是体检与提交包共用的正文身份检查。`章节卡.identity_errors` 只检查 ID 与展示号，允许全局改稿核对旧卡而不要求快照回到旧章。

体检只对显式 `migrated_from` 项目且永久 ID 小于 `archive_required_from` 的旧章允许缺省展示章号、视角与字数，已有字段仍校验。新章提交和本次改过的修订正文始终使用完整检查，不以展示顺序放宽旧格式范围。迁移本身保留正文原字节。

`revise --review` 根据当前候选和完整有效基线生成 `_候选/REVISE_关联核对.json`。正文内容变化触发事实、状态、规划与后续正文核对；设定或运行账本变化保守列出现有正文。核对按文件职责分组，不是语义依赖解析。每组状态和说明填写后才允许试算给出批准摘要。核对文件的摘要参与 REVISION_SHA256；基线或候选改变使旧核对失效，核对说明改变使批准失效。提交记录保存到 `06_归档/修订核对_<摘要>.json`，与正文一起回退。

章节重心与工作模式分开。没有新字段的旧卡按完整对应情节推进、过渡对应体验呈现解释。体验呈现不要求冲突、代价和不可逆变化，仍检查具体信息、连续性和明确选择。

## 3.9 写作质量试验

`02_检查层/14_写作质量试验.md` 是按需调阅的系统手册，`03_读者层/10_试改记录_空白模板.md` 是只供复制的空白模板。二者均加入母版必需、新项目必需和系统同步清单。填写后的试改记录属于创作数据，保存在探索目录；正式留存沿用 `06_归档/修订记录_*.md` 的已有修订范围。

两遍检查只携带短版诊断与修改指引。读取包不自动装入完整手册、模板、探索稿或反馈记录。原有 `03_读者层/09_阅读驱动力记录.md` 继续由作者维护，不纳入覆盖同步。新版本没有新增 CLI、评分门槛、候选权限或自动采纳动作。

发布验证在母版外部建立临时新书与旧版同步样本。采用逐文件差异清单更新母版，验证日志、合成样本、旧书正文和专名扫描表保留在外部工作目录。

## 3.9.1 展示资料与新书隔离

`docs/` 是项目介绍，原地开书会保留，但不进入创作读取包。以下排除规则仅用于可选的 `init` 另建目录方式：`新建项目.py` 的 `母版专用目录` 在遍历复制源时排除整个展示目录，包含页面和本地图片。`发布自测.py` 通过实际初始化检查新书没有此目录，不再仅检查发布清单中是否声明了展示页。

展示页使用 `data-system-version`、`data-plugin-count` 和 `data-release-tests` 标记实际显示的值，测试解析 HTML 后与母版版本、插件清单和本轮发布结果比较。页面内部锚点、本地资源、唯一 ID 和图片替代文本一并检查。这些检查不代表浏览器视觉效果已经通过人工评审。


## 3.10 可选开书探索

研究手册、空白模板与开书探索模块列入 sync_files。填写后的 `06_归档/开书研究.md`、`文风推荐.md`、`开书材料清单.json` 均为可选用户数据，不列入必需文件或同步种子，旧书不使用时不产生研究状态要求。

`discover --prepare` 在探索目录建立记录，import 保存按 SHA256 命名的原始材料，stage 只向 INIT 或 REVISE 增补候选。任何已有不同内容均拒绝覆盖。研究记录进度独立于 project.json 的 initialization，不写后者。正式归档沿用原有批准摘要，完整材料一并绑定，备份收取正式归档；读取包不收研究全文。

开书资料仅放行固定前缀、完整摘要和有限扩展名，单份限制 20 MiB，拒绝符号链接、空材料和摘要不符。修订的二进制支持仅限该类材料；正文与其他文件仍要求 UTF-8。导入不执行、不解压、不联网，不改变材料的证据等级。状态只描述记录，不能证明结论准确。

`voice --recommend` 读取当前共创候选的创作意图与设定，输出代理任务和按需调阅路径，DATA 明确隔离。6.0 的 CLI 样本核验接续当前候选并标明来源；正式文风基线仍只读取正式样本。推荐与试写提示不改变 D10 或文风基线。完整推荐记录不进入每章包。

## 3.11 结构复盘接入

`结构复盘.py` 实现 `outline`、`outline --prompt`（只读）、`--prepare` 和 `--refresh`（持既有项目写锁，仅候选）。单一记录是 `06_归档/结构复盘.md`，按需进入 INIT 或 REVISE，没有新增 project.json 生命周期。状态、间隔及隐藏 JSON 资料版本受格式检查，状态自述不作为章节或开书质量闸门。刷新保留判断文字、重置待复盘，并使旧候选摘要失效。

记录里的资料版本比较创作意图、固定设定、大纲非章节区、未写章节行与先前定稿正文；正常章节状态变化与新增正文单独计数，使用永久 ID 集合与展示顺序，不用最大 ID 推算已写章数。提醒不保证语义正确，未覆盖依赖仍须回读。

原大纲模板增加可选阶段规划区，位于章节表之后、大纲变更记录之前，避免混入初始化路标计数；旧项目大纲不在 sync_files，保持原字节。基础必填、D01 至 D10、schema 2、逐章白名单与修订关联核对保留。本轮只给 foundation/revision 白名单加一份复盘记录。

完整手册与记录不进入每章包。两遍检查携带短版结构核对；status 与读取包控制台给非阻断提醒。实际方案需在原大纲的近期行及章节卡中展开以进入正式包，复盘报告不能成为隐含的第二套计划。

## 3.12 增量

`feedback` 查询或追加运行问题，`运行反馈.py` 保存来源、状态与处理事件，采用共享锁和原子单文件替换，不更新创作 HEAD；备份包含日志。`speculative_checks.py` 为显式启用的插件提供结构校验，不判断语义。

`outline --scope small|large` 支持三章/十二章审查，绑定连续定稿正文的版本和顺序，大审查合并小审查。CLI 不联网，榜单正文研究由执行代理实际执行。`阶段反馈自测.py` 验证日程、兼容、反馈日志和升级保护。

共享文件基线写入新项目 manifest，已知上一版本基线支持首次迁移；无法识别的差异或本地修改默认停止。定制插件保留包、版本和检查；明确保留或替换需逐项指明。同步计划在事务锁内再次核对目标摘要，规划期间有变化则拒绝写入。

## 3.13 阅读与反馈接口

`outline --reading --scope small|large --offset N --limit M` 是只读正文窗口，M 为 1 至 12，默认 4。依展示章号读取正式稿，范围包括漏审和前一章衔接；每批输出原文件 SHA-256 与下一偏移，仅去除首行流程元数据。文件缺失、非法编码、符号链接或参数越界均报错，不以截断充当完成。

`运行反馈.quality_followups` 按最后更新时间选择未关闭的质量、目标与研究问题。阶段任务输出有界摘要作为 DATA，逐章包只增加编号；日志异常显式报告。历史已填表结构保持兼容。

`修订.baseline` 验证运行日志后单独排除它，`collect` 拒绝把日志列为修订目标。其他有效文件与 HEAD、候选和核对结论仍绑定；提交锁内重复校验。日志不参与修订或回退的写集。追加记录可与未变的候选并存，不表示新用户意见可忽略。

3.13 同时纠正两项状态提示。探索目录保留为资料，不当作未提交章节提示删除。文风基线记录正样本、认可名单及认可正文的来源摘要；普通定稿增长不触发重建，撤销认可或修改已认可正文会提示更新。语料不足时明确暂不建立统计基线，旧基线无来源摘要时提示按需核验，不强迫补认章节。

## 3.14 阶段作者访谈与审读结论

统一 CLI 新增互斥只读模式 outline --interview，与 --prompt 共用阶段任务，按当前复盘范围显示提示；不读取外部聊天、不自动生成总结或答复。新模板的 STRUCTURE_FIELDS 增加作者访谈状态、依据和意见处理；三章/十二章记录自述已复盘时核对状态已处理且说明非空，开书为不适用。旧记录缺字段按兼容路径读取，不修改旧书。

prepare 开始新轮次，不继承旧轮答复为本轮完成；同范围已归档但未完成的复盘则保留原问题与意见，继续同轮交流。refresh 保留文字，比较核心、规划、正文、顺序及新增章节；依据或范围变化后，已交流或等待答复的访谈恢复待核对，无变化不要求重复提问。状态、依据均是操作者陈述，不能证明真实交流，也不引入独立批准状态或章节提交硬阻断。正式复盘与采用的安排仍经原 REVISE 归档。

feedback --issue 显示最新追加事件在前、历史在后。事件结构和摘要算法不变；报告位置、正文版本、结论范围及补充/替代关系记录于已有来源、验证和范围字段。工具不自动推断外部报告的新旧语义，操作者须核对。


## 3.15.1 的读取与落盘复用

结构复盘的 ReadContext 仅在一次只读调用中传递，项目、正式与候选路径隔离，不跨命令缓存，也不提供文件系统一致性快照。文风提醒仍核对当前认可来源，只省去分布计算和无关正文读取。体检一次建立梗概索引，保留旧编号及文件后缀范围。

修订的完整候选与全项目基线继续参与摘要批准，影子仍生成并体检；最终写入去除未变化文件。图谱的新鲜度输入由体检.graph_tracked_paths 共用，依赖账本实际变化或原图谱已旧时，即使渲染字节相同也刷新图谱，避免破坏修改时间契约。原子替换默认保留已有普通文件的 POSIX mode，明确传入的 mode 优先，新文件仍使用原私有默认值。

## 3.15.2 的诊断与分页边界

第二遍检查把待核对来源与可见矛盾分开，必需输入缺失仍停止相应检查；不改变模板字段、解析器、候选范围或摘要批准。`outline --reading` 的尾句使用本次起止位置说明分页覆盖，不维护跨命令阅读会话，也不证明实际读完。

发布清单增加真实 3.15.1 共享文件基线，原清单与策略文件保持。新测试扩展现有阶段反馈套件，不进入作品同步范围。

## 4.0 读者引擎层与三章一批

**批次范围**：`引擎.解析批次` 把 `K0019-K0021` 按分章大纲的展示章号展开成连续章节，章数不超过 `project.json` 的 `batch_max`（缺省 3，硬上限 5）。单章写法完全不变，单章候选的摘要格式也不变。

**批次提交**：候选目录为 `_候选/K0019-K0021/`；允许写入的路径取批内各章白名单的并集。候选摘要的 payload 在批次时多一个 `chapter_ids` 字段。影子里对每一章各跑一次 `提交包.py <影子> <章> --批末 <末章>`，任何一章失败即整批拒绝并点名；锁内复核与正式复核同样逐章执行。事务标签为 `batch commit <批次>`，`rollback` 整批撤销。

**快照口径**：批内非末章的“快照抬头已推进”检查改为核对批末章，其余逐章检查不变。

**引擎层启用**：`引擎.启用` 只看 `00_设定层/04_读者引擎.md` 的核心栏是否填写：6.0 通用模式核对读者承诺、主要阅读期待、至少一个具体阶段；升级对抗模式及没有模式字段的旧卡核对读者承诺、触发/兑现/放大、两级升级阶梯、一个对手。启用后读取包把引擎卡、覆盖本章或批次的弧卡、悬念账排在最前面（DATA），提交闸门对**弧卡范围内**的章节核对悬念账的章末钩子行。

**读取包的 CONTROL 预算**：08 两遍检查、执行契约、代理协议的包内摘要、文风基线与项目配置 §一–§二。四点零引擎组直接量批次成品包，CONTROL 超过 5,000 字符即失败（R91）。

**开书 D11**：`开书.必需决策` 增加「读者引擎」；`核开书内容` 同时核对引擎卡本身，决策表写“已确认”而卡还是模板时逐项报缺。

**章节卡 v4**：`章节卡.是新版` 以「## 三拍 🔒」一节为标志，走 `_validate_v4`；3.x 旧卡继续走原分支。重复证据的核对抽成 `_repeat_errors` 两个分支共用。

**复盘**：带「下一段加强措施」一栏的新记录，标已复盘时这一栏必须写出具体安排或「保留：理由」（R89）；大审查的榜单研究状态新增「本轮不做」，须在依据栏写明理由。

**同步**：`04_读者引擎.md` 与 `05f_悬念账.md` 是种子文件，旧项目缺失时补入空模板、已存在不动；`引擎.py` 与弧卡模板随共享文件同步。`previous_sync_baselines` 新增 3.15.2 的共享文件哈希。

## 4.1 题材文风档

**登记表**：`04_题材插件/文风档/registry.json` 列出四档（literary、webnovel、romance、genre），每档一份 md，含「写作约束」与「语言检查（第二遍）」两节，并声明 `allow_categories`（去 AI 腔母版通用层里对本档不算问题的类别）与 `expressive`（是否做表现力漂移提醒）。`plugin_default` 只是题材插件的推荐。

**选择**：`project.json` 的 `voice_profile`，由 `voice --profile <档>` 经事务写入；`v2_core.VOICE_PROFILES` 校验取值。缺失时按 literary 处理，行为与 4.0 相同。新书开书试算要求显式选择。

**进包**：读取包把所选档的 md 作为 CONTROL 装入，替换原 `11_文风基线.md` 的通用约束一节；项目缺登记表（未同步的旧项目）时只提醒、不带档。四点零引擎组与文风档组都直接量成品包，CONTROL 不超过 5,000 字符。

**去 AI 腔**：`文风.载入词表` 按 `allow_categories` 过滤母版通用层 `AI腔词表.txt` 的类别；本项目词表与 `允许AI腔.txt` 照旧生效。

**表现力**：`文风档.表现度量` 粗略统计每千字的情绪直写、内心独白、旁人反应；外放档在 `style` 里对照正样本，本章任一项低于样本一半即提醒“正在往克制漂”。只提醒，不拦提交，数字可以刷，不作为交付条件。

**定声**：外放档在五个文学维度外另加情绪外放度、内心独白量、反应放大、口语与网感、高光落地方式五个维度；正样本建议 3,000 字，使文风基线从开书即可建立。


## 6.0 数据保护、工作流与创作模式

章节提交成功后将候选原子移到既有排除区 `06_归档/_候选存根/`，保留打开文件句柄的新写入；输出实际位置，不无条件递归删除。存根不计作待提交候选，也不进入普通备份。

备份对目录遍历失败报错，在同一事务锁内取材，核对采集前后集合和内容；未完成事务的现场备份保持混合状态标记，不擅自恢复。插件变更在锁内重读配置并计划，多次 rollback 的有效性按完整撤销关系计算。

读取包先按展示顺序选定必需前章，再验证文件。简报和正式包都按展示号展开范围。状态与体检识别单章和批次候选；探索仍只作为资料提示。文风导入写 INIT／REVISE，CLI 核验说明候选来源；文风基线公共函数默认正式来源，避免未批准文字污染统计语料。

引擎卡模式存于创作文件中：通用检查读者承诺、阅读期待与阶段发展；升级对抗沿用原爽感、升级和对手检查。缺模式的旧卡沿用旧格式，未知或重复模式报错。模式变化参与原 D11／修订摘要，不新增 project 字段或 schema 迁移。三拍等现有表头保留作为格式，通用语义允许体验和关系发展。

发布清单加入原 5.0 真实共享文件哈希，三份专项自测只属于母版；新增测试文件必须同时登记母版清单和新建排除表。升级兼容需验证用户文件与定制插件保留，不把软件同步当作品改稿批准。

## 直接打开下载目录

默认使用入口为下载文件夹加一句开书提示词。代理读入口协议后，对用于创作且没有 `project.json` 的完整下载目录运行 `start`；软件审查和母版维护仍运行 `doctor --template`。

`start` 在同一事务锁内检查清单、路径和已有数据，创建项目身份与同步基线，并将三份母版判定文件移入事务历史。它不改设定、正文、用户附加材料或 Git 配置；已有有效项目仅查看状态，损坏身份和未完成事务不自动重建或恢复。当前文件夹直接用于作品，不另建副本，不要求用户运行脚本。

`入口自测.py` 在临时目录验证首次启动、重复调用、缺失文件、链接路径、旧项目与故障回退。文档和 CI 的源码维护仍使用未开始创作的下载副本。原 `init` 保留给维护工具与兼容流程，属于可选命令；其中的新书文件排除规则不适用于 `start` 保留下载内容的方式。
