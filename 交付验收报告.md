# 入口简化更新 · 2026-09-23

默认入口改为下载整个文件夹，在 AI 代理工作区打开并发送一句开书提示词。代理通过 start 原地登记项目，旧的 macOS 快捷脚本已移除；已有项目不重新初始化。以下为入口调整时记录的验证结果。

| 本轮验证 | 结果 |
| --- | --- |
| macOS / Python 3.9.6 完整发布自测 | 446 / 446 通过 |
| macOS / Python 3.14.7 完整发布自测 | 446 / 446 通过 |
| 直接开书专项（两种 Python） | 各 9 / 9；包括首次登记、重复接续、原稿保留、缺失/损坏输入、并发及中断恢复 |
| 中英文网站 | 提示词复制与成功反馈通过；1280 / 320 像素视口无横向溢出 |
| 公开文件清单及已知私有模式 | 162 份文件；已知私有模式零命中 |

开书专项只在临时副本执行，未在维护母版或真实小说中创建项目身份。代理准备项目使用现有事务机制；正式章节仍需要原有候选检查与同一摘要批准。这些结果不代表所有 AI 平台的兼容认证。远端运行状态以仓库 Actions 对应提交为准。


## 验证范围

发布自测覆盖新书、读取包、候选、插件、提交、回退、恢复、备份、并发、同步和兼容路径。专项子检查不重复加到总数。测试使用隔离副本；母版和实际作品分别维护。

当前版本沿用 schema 2；共享文件通过清单同步，正文和已填资料不由版本升级自动改写。历史作品的单独测量与操作记录不随公开包分发。

## 限制

本版仅在 macOS 上完成本地使用验证。CI 中的 Ubuntu 测试不能替代完整使用流程的跨平台验证；Windows、WSL、网络盘及断电场景不在本地实测范围。移动端网页检查为视口模拟。

测试通过仅说明列出的软件行为符合断言，不证明全量代码覆盖、任意并发安全、文学质量或创作收益。公开文件扫描只识别已知模式，不能证明不存在所有未知隐私或版权问题。
