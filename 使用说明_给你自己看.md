# 使用说明

这套系统适合需要连续维护人物、关系、秘密、物件、伏笔和章节顺序的长篇小说。它不会替你决定故事，也不会把检查通过当成作品好看。

4.0 起，你要拍板的地方集中在两处：**这一弧往哪走**（批准弧卡），**这一批读着想不想往下看**（连读三章后批准）。其余的记账、核对和事务由系统和 AI 完成。

## 开一本新书

下载并解压系统，把整个文件夹拖进 ChatGPT Codex、Claude Code 或 WorkBuddy 等 AI 代理的工作区，发送这句话：

> 请先阅读这个文件夹里的 AGENTS.md 和 START_HERE.md，按其中的流程引导我开始创作一本小说。

然后跟着 AI 的引导开始。它会检查环境、准备当前文件夹，再与你讨论已有想法和合作方式。用户不需要运行快捷脚本。如果不想每次下载，可以在开始写作前备份一份空白模板。以后开新书时，复制这份模板即可；也可以从 GitHub 重新下载。

当前文件夹就是这本书的项目。所用 AI 需要能读写其中的文件并运行 Python；普通聊天附件模式不能完成这套流程。目前仅在 macOS 上完成验证，需要 Python 3.9 或以上。Windows、Linux 与 WSL 等其他环境尚未验证；如需使用，请自行适配并测试，确认文件保存、备份和恢复正常后再用于正式作品。云端工作区还需确认项目可以完整保存或导出。

## 日常接续与完整接手

下次继续写，打开已有文件夹，告诉 AI「请先读 AGENTS.md 和 START_HERE.md，接续这本书」。代理会检查状态，阅读最近三章及必要衔接，并补足到期或失效的审查；不会每次重新开书。

需要通读全书或完整接手时，直接说明阅读范围即可。AI 应报告已读、未读和下一起点，不用大纲代替正文。只要求接手检查，不等于授权续写或改稿。

## 开书：写第一章之前

新项目的开书状态是 `draft`，这个状态下 AI 写不了第一章。

这道闸门是给你挡一件很具体的事：你说了个题材，模型顺手把一句话故事、核心问题、人物档案、结构路标全填满了，看起来完成度很高，你在第 8 章才发现这本书的内核根本不是你想写的。**字段填满挡不住这个。**

先决定**合作方式**，这一项决定了后面 AI 能做什么：

| 取值 | 你怎么参与 | AI 能做什么 |
|---|---|---|
| `author_led` | 你提供核心方案 | 整理、追问、指出冲突。不代拟 |
| `guided` | 从几个差异明显的方案里选、混、改 | 给候选，解释差异，不替你选 |
| `ai_draft` | 你明确委托它起草 | 可以代拟，关键决定逐项列出，可按明确范围合并确认 |
| `import` | 你已经有大纲或圣经 | 拆解、校验、补问缺项 |

**说了题材不等于授权 AI 决定别的。** 沉默和跳过不算批准。针对明确展示的方案回复“可以”，可以确认该方案；已授权的范围会继续有效。

然后走和章节提交一样的三步：

```text
python3 novel.py foundation --prepare
python3 novel.py foundation
python3 novel.py foundation --apply --approve <试算输出的完整摘要>
```

试算会一次列出全部缺项，并把每份文件的摘要摆出来。你确认的对象是那份清单。

十一项决定必须都确认过才放行：合作方式、核心创作承诺、不可替代元素、主角核心欲望、核心困难选择、结局方向、创作禁区、结构路线、暂定书名、文风方向、读者引擎。

定文风时，先选一个**题材文风档**（4.1）：文学克制、网文爽文、言情细腻、类型紧凑。它决定这本书用什么口气讲——网文档允许情绪直说、内心吐槽、爽点给足旁人反应，文学档就是《潮汐表》那种克制。选法：`python3 novel.py voice --profile webnovel`。

最后一项是**读者引擎卡**（`00_设定层/04_读者引擎.md`）：6.0 默认通用模式，写清读者期待与关系、认识、处境或体验如何发展；选择升级对抗时，再明确爽感兑现、升级与对手。模式与其他决定一起确认，不多一次批准。它会放在每一次写作材料的最前面。没有它，AI 只能把“没写错”当目标，写出来每章都对，连起来却不想读。它们记在 `06_归档/开书决策记录.md`，每一条都要写来源——是你自己提的，还是 AI 提了你选的，还是 AI 代拟你批准的。

说清楚它的边界：**它证明不了这些决定真是你做的**，只能证明它们被逐条提出过、有来源、在你批准的那一刻全部确认过，而且你看到的清单在写入前没被换掉。

暂定书名和结构路线以后都能改，走独立维护事务。

你还要明确题材插件选择。

```text
python3 novel.py plugin none
```

也可以安装一个或多个插件。装了插件之后，每个插件给你两样东西：

- **逐章检查**跟着读取包进每一章，AI 自动执行，你不用管
- **题材手册**不进包，写不下去或吃不准时自己调阅

```text
python3 novel.py guide suspense
```

手册里是判断依据——比如"红鲱鱼要不要在结尾解释掉""内部障碍和外部障碍哪个该更重""这个新能力会不会给第 12 章开反向漏洞"。这些东西每章都带着太浪费，需要时一条命令就能拿到。

**插件里的数字都是警报值，不是硬规则。** 越线了说明理由并记一笔，不是禁止。

```text
python3 novel.py plugin list
python3 novel.py plugin install romance
```

## 你真正要决定的事

**每一弧（5—10 章）一次**：看弧卡——这一弧要发展什么、人物关系与认识怎样变化、阶段末给读者什么，以及每章怎样承接、发展和落下。选择升级对抗时再看对手、翻转、低谷与高潮。可以改、可以换，批准后这一弧就照它写。

**每一批（三章）一次**：连着读完三章和一段三拍摘要，只回答一个问题——读到这里，想不想点下一章？想，就批准；哪里不信、哪里想跳，直接说。

开篇三章和弧末高潮仍然一章一批，你一章一章看。

系统可以整理记录，并在你的明确授权内代拟。

## 开新书时怎样试用写作质量流程

先正常完成开书与草稿，再从前三章或首个完整叙事段选一至三章，观察人物对话、日常体验或主要行动是否达到你的预期。不必每章做试验。

让 AI 先指出具体段落的问题和原稿值得保留的部分，再集中改一至两个阅读问题，并完整复读、核对关联。需要比较时保留原稿，分别做常规检查稿与定点改写稿。字数增加、套话减少、检查通过都不能直接证明更好。

方法见 [写作质量试验](02_检查层/14_写作质量试验.md)，记录用 [空白模板](03_读者层/10_试改记录_空白模板.md) 的副本。没有外部读者也能继续写，由你决定取舍并记录为作者判断。实际反馈和试稿都留在新书或独立试验目录，不回填系统母版。

## 一批怎样完成

1. 这一弧的弧卡已经由你批准（`arc --new` 建卡，`revise` 批准）
2. AI 做好这一批三张章节卡
3. 生成一个批次读取包
4. AI 在同一次对话里把三章连着写完，每写完一章先存草稿
5. 两遍检查：先三章连读查“想不想往下读”，再逐章查连续性与语言
6. 把三章和账本一起放进批次候选区，运行一次试算
7. 你连读三章和三拍摘要，确认后一次提交

命令如下。

```text
python3 novel.py arc --new A02 K0019-K0027
python3 novel.py package K0019-K0021 --write
python3 novel.py hooks K0019-K0021
python3 novel.py commit K0019-K0021
python3 novel.py commit K0019-K0021 --apply --approve <试算输出的完整摘要>
```

`hooks` 把三章的最后几百字和登记的章末钩子摆在一起，方便你判断“为什么会点下一章”。一章一章写时，把范围换成单个永久 ID 即可。

试算的最后一行会给出一条可以直接复制运行的正式提交命令。

试算后先完整展示待批准正文和候选清单，把完整摘要与确认对象一并保留。候选或正式基线发生任何变化，原摘要都会失效。看到事务编号后，本章才算正式进入项目。同一稿已经完整展示并按同一摘要批准、提交结果一致时，交付结果和正式文件即可，不默认再次重贴正文；你需要重看时仍可完整展开。


## 全局修订与撤销

改旧章、固定设定或创作意图时，先选文件建立修订副本。

```text
python3 novel.py revise --prepare --file 00_设定层/01_固定设定.md --file 05_正文/K0001.md
```

编辑 `_候选/REVISE/` 中的副本，把受影响的账本、章节卡和摘要一起加入，再预览并提交。

```text
python3 novel.py revise
python3 novel.py revise --apply --approve <REVISION_SHA256>
```

试算会给出差异、摘要和体检结果。批准后形成一笔 `global revision` 事务，并重建图谱。只有这笔修订仍是最近一次提交时，`rollback` 才撤销它。普通文件编辑不会自动登记；已经直接改过正式文件时，应从备份恢复到新目录再比对。

修订白名单保存在 `revision-policy.json`，仅覆盖创作内容和已启用插件的用户文件，不允许修改工具或系统检查。它不物理删除章节，废弃章节在大纲中保留 ID。详细流程见 `02_检查层/10_全书改稿流程.md`。

## 出现异常时

先运行下面两条。它们只读，不会改你的稿子。

```text
python3 novel.py status
python3 novel.py doctor
```

如果它们报告"存在未完成的事务"，先看清楚涉及哪些文件，再决定要不要恢复。**系统不会替你恢复**——恢复会改写正式文件。确认之后：

```text
python3 novel.py recover
```

如果最近一次正式提交本身有问题，可以回退。

```text
python3 novel.py rollback
```

回退也会形成新的事务记录，历史不会消失。**注意 `rollback` 自己也是一笔提交**：连按两次是重做，不是再退一章。要确认哪些提交现在生效，看 `history` 里的 `← HEAD` 和"已被撤销"标记。

## 规划简报：一次准备几张章卡

一章的工作分两半，只有前一半能批：

```text
python3 novel.py brief K0008-K0010 --write     ← 一次，规划三章
```

用这一份和 AI 把三章的章节卡都做出来。顺带的好处是你能看见这三章连起来的弧线，而不是每次只看一章。

4.0 起生成也能按批：三张卡齐了，出一个批次包，让 AI 在同一次对话里把三章连着写完。

```text
python3 novel.py package K0008-K0010 --write
python3 novel.py commit K0008-K0010            ← 试算，一个摘要
```

同一次对话里，前一章的真实正文就在 AI 眼前，比账本更准。你连着读三章、看一段三拍摘要，批准一次，三章一起落盘；不满意可以整批 rollback。一批默认三章（`project.json` 的 `batch_max`，最多五章）。开篇与作者指定的关键章可以一章一批。

## 章节卡不用再手抄检索了

3.6 起重复动作清单交给工具：

```text
python3 novel.py repeat K0008 --card
```

它自己扫最近三章、列出跨章重复的动作和对白骨架、给出章末落点形态，最后输出一个五行的结论块。把那一块整体替换掉章节卡「重复动作清单」的表体——重复证据状态也在块里，不用再去别处填第二遍。你要做的只是看一眼「本章处理」那行合不合意，不合就改。

手工抄写检查结果会增加重复劳动，现由工具生成后核对。

## 写不完目标字数时

`status` 会告诉你按当前节奏能写到多少字。以下为合成示例：

```
字数进度  已写 12,000 ／ 目标 60,000（20%）；章节 6／20（30%）
          平均每章 2000 字，按此推算完稿约 40,000 字
```

偏离超过 15% 会提醒。**它不会替你补字数**——提醒里给的是"改目标、加章数、或者接受较短的成书"，不是"每章多写点描写"。

## 去 AI 感

无论使用哪种 AI 工具，都可能出现**读起来顺、但不像这本书**的段落。审阅时除了核对事实，也要与本书认可的文风样本对照；通顺不代表符合作者的表达习惯。

做法分三步。

**一、开书时建基线。** 正样本填好之后跑一次：

从 3.6 起，**已定稿的章节不会自动进基线**。模型写的正文默认不是文风样本——否则第一章的模型腔会变成后面所有章的标准，而你察觉不到。你认可的章节写进 `02_风格样本.md` 的「认可为文风样本的章节」那一行。


```text
python3 novel.py style --baseline --apply
```

它从你的正样本、加上你在 `00_设定层/02_风格样本.md` 里**明确认可**的那几章，算出这本书的句长分布、比喻密度、感官偏好这些数字，写进 `02_检查层/11_文风基线.md`。已定稿的章不会自动进来，不然模型写的第一章就成了后面所有章的标准。这不是通用标准，是**你这本书自己的尺子**。语料不足两千字它会直接说基线不可靠、只报套话命中，不给你一个站不住的数字。

**二、生成之前，约束跟着读取包进去。** 这一步不用你做。`11_文风基线.md` 是 CONTROL，每一份正式读取包都带着它。在 Kimi 的对话里说"别写得像 AI"，下一段就忘了；写进读取包，它每章都会重新出现。

**三、写完之后定点改。**

```text
python3 novel.py style K0001 --draft _候选/K0001/05_正文/K0001.md --prompt
```

它给你三样东西：命中模型套话的具体行号、本章相对本书基线的偏离、一段可以直接贴给模型的定点改写提示词。

定位结果先进入本轮共同的问题清单。各遍检查引用同一位置与证据，不重复写几套诊断；全部诊断后优先局部处理经阅读确认的问题。需要改场景时说明影响，改后回读因果、信息与完整正文。正文有变化时再跑定位，正文、词表与基线均未变时复用本轮结果；有意保留的命中不要求清零，也不靠命中数下降判断交付质量。

换平台之后发现一批新套话，把那几章一起喂进去：

```text
python3 novel.py style --suspect 05_正文
```

它拿这些稿子和你自己的语料比词频，列出"模型爱用、你不爱用"的搭配。挑出确实是套话的加进 `_工具/AI腔词表_本项目.txt`。

最后说句实话：**最大的杠杆是那份正样本。** 如果它是你自己写的两千字、硬规则是具体的五条，这两样本身就比任何检查器管用。工具的作用是把它变成可度量的尺子，并且在几十章之后告诉你有没有漂——它解决不了"你还没想清楚这本书该是什么声音"。

## 每十章左右封一次段

读取包保留阶段摘要、最近若干章完整条目、较早条目的截取预览和显式标记的长期保留项。旧条目不再全文进入包；需要细节时调阅原条目或在章节卡声明旧章。定期封段仍可减少历史预览的累计体积。

`status` 只展示事实资料存量，实际读取压力看本次 `package` 的成品用量。存量增长或写到整十章不自动要求封段；需要压缩实际读取压力、且这段事实能够安全提炼时再做。先把该留给后文的内容写成阶段摘要（标题写成 `### 记忆段 K0001 至 K0010 阶段摘要`），再让工具把详细条目搬走。

```text
python3 novel.py seal K0001 K0010
python3 novel.py seal K0001 K0010 --apply
```

摘要必须你或 AI 先写。工具只负责搬运——它不知道这一段哪些细节以后还要用。

## 备份

```text
python3 novel.py backup --note "第十章完成"
```

备份保存在项目的 `_备份` 目录。恢复时必须指定一个尚不存在的新目录，工具不会覆盖现有项目。

备份不含 `.novel` 事务历史。正常完整备份恢复并核对后可继续写，`history` 与 `rollback` 从新目录重新开始；中断或损坏事务的现场备份可能包含混合状态，只用于保全现场，不能据此保证可继续写，也不能替代原事务恢复资料。

项目里有符号链接时备份会**直接拒绝**并指出具体路径。这不是挑剔：带链接的包恢复不回来，而 v3.0 会给你一个看起来成功、其实校验不过的备份。把链接换成真实文件再备份。

## 旧项目升级

先在新版母版目录试算。

```text
python3 novel.py migrate "/旧项目路径"
```

确认计划后执行。

```text
python3 novel.py migrate "/旧项目路径" --apply
```

迁移会留下事务记录。详细边界见 `迁移说明.md`。

**v2 和 v3.0 的项目升级到 v3.1 只需要 `sync`**，结构版本没有变，不用再走一次 `migrate`。

```text
python3 novel.py sync "/项目路径" --check
python3 novel.py sync "/项目路径"
```

## 3.8 的三处使用变化

1. 修改旧章正文、设定或账本后，先运行 `python3 novel.py revise --review`，打开 `_候选/REVISE_关联核对.json`。工具自动列文件，你只对每组写明修改或保留理由并标记已核对，再试算和批准。候选或项目改变后需要重新核对。
2. 观察、回忆、日常相处等章节可以选择“体验呈现”。过渡模式默认如此，完整章也可以选；不强制凑冲突、损失或不可逆事件。
3. 想先找到人物或声音时，可以授权 AI 在 `_候选/探索/` 试写，再决定是否采纳。临时稿不进入正式记录或系统备份，需要长期保存时另存。

旧项目同步后，已有正文头缺项或身份不一致会被明确报出，应按原大纲修复，不能编造新身份来过检查。正文和用户设定不会被 sync 自动改写。


## 3.10 开书探索与文风推荐

开书时先提供已有研究报告、故事方案、参考作品和试写；没有材料再按需研究。已有方案或明确文风可以沿用，不新增研究门槛。`discover --prepare` 准备可编辑记录，`discover --import <文件>` 保存原始材料副本，`discover --prompt` 给出研究任务。`voice --recommend` 给出文风推荐任务，两者本身都不联网或生成推荐结论。

需长期保留时使用 `discover --stage`，研究记录与原始材料进入现有 foundation 或 revise 候选；按原有完整摘要批准后才正式归档并进入备份。研究报告、文风推荐及材料清单不整份进入逐章读取包，只有作者采纳的简短约定进入创作意图与风格样本。方法与空白模板可以升级，填写后的记录和原始材料不被同步覆盖。

具体方法见 `02_检查层/15_开书探索与文风推荐.md`。导入只能证明材料保存成功，不能证明已经阅读、内容可信或作者已采纳。推荐可能不合适，研究可能耗时；可以否决、暂缓、缩小范围或跳过，不能保证提升文学质量或收入。

## 大纲怎样支撑长篇

开书时让代理先论证故事如何持续展开、中段是否有实质发展、人物选择与转折是否站得住、结局是否准备充分，以及篇幅是否有内容支撑。已有报告和充分讨论继续沿用。D08 确认后分阶段规划，近期详细、远期留方向，不要求一次填满全书每一章。

写作中每三章做小审查、每十二章做大审查，榜单正文研究可选，不做写明理由，阶段结束或重要偏离可提前触发；审查时，比较原先预期与正文实际。使用 outline --prompt 获取问题，需要留痕时用 outline --prepare，在现有候选中修改和记录。每轮先给你一段故事小结，再开放询问后续方向、人物动机和想保留或调整的内容。已有覆盖本轮的答复会接续；等待你的回答后再决定依赖它的剧情，明确跳过时记录边界。十二章只交流一次。outline --prompt 已含同轮访谈提示；只有单独接续交流、需要重看任务时才用 outline --interview，工具本身不会替你回答。核心和后续路线改动沿用 revise 及关联核对。复盘也可以决定保持路线，不把不断修改当成进步。同一问题的证据、处理和下轮验证集中在原复盘记录的一张行动表里，其余小节引用位置；不用让你重复填几张近似表格。

运行问题无需你重复填表。代理会从可见对话提取纠正、返工和故障，记录来源、处理和验证；`python3 novel.py feedback` 查看摘要，`--issue <编号>` 查看完整过程。没有证据的判断不能当事实，正常创作取舍也不必升级系统。记录留在各自作品，母版保持空白。
