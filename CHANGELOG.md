# 版本记录

记录功能变化、兼容性与验证范围。历史版本按技术变化概述，不包含真实作品的进度、使用者答复或操作记录。当前用法以 START_HERE.md 为准。

## 6.0 · 2026-09-23

- 默认入口改为下载并解压系统，在 AI 代理工作区打开整个文件夹，发送一句开书提示词。`start` 原地登记，已有有效项目只接续；损坏身份和中断事务不自动重建或恢复。
- 移除 macOS 快捷脚本。首次开书和日常接续均由代理入口完成；`init` 保留作维护与兼容用途。
- 新引擎卡默认采用通用模式；升级对抗模式需明确选择。旧卡没有模式字段时保留原解释，坏格式和重复字段报错。
- 章节候选成功后原子移入候选存根，保留复核期间与编辑器后续保存的内容。
- 备份遇到不可读目录停止；在事务锁内核对文件集合与内容。中断现场备份单独标记。
- 插件安装、不启用和卸载在锁内重算计划；多层回退按完整撤销关系显示生效状态。
- 正式读取包缺必需前章时停止；状态与体检识别批次候选；多章简报按展示顺序解析范围。
- 文风导入进入 INIT 或 REVISE 候选，正式基线只使用已提交样本；存在多个候选时拒绝猜测目标。
- 统一榜单正文研究的可选口径；增加直接开书、数据保护、工作流与模式兼容专项。
- 提供中英文项目介绍、公开文件白名单、可复验打包脚本与 CI/Pages 配置。公开署名为 LISIYUAN。
- 公开说明仅保留技术依据与验证结果，更新仓库和网站地址。

结构版本仍为 schema 2，旧书不需重新开书。共享文件通过 `sync --check` 审阅后升级；正文、已填设定和本书资料不自动覆盖。实测范围与限制见交付验收报告。

## 5.0

增加人物声音表与对白统计工具：按角色查看句长、问句率、语气词等分布，支持按弧比较。声音表仅在有效填写后进入正式读取包；空模板不改变旧项目内容。覆盖率不足时明确说明样本限制。统计只用于定位，不证明文学质量。

声音表纳入开书与修订白名单，不由章节批次直接改写。增加读取包和体检集成测试。

## 4.2

精简可选审计字段与快照表格；事实记录改用短条目。批次独立审稿与弧末通读纳入流程。只读验证忽略 Python 字节码缓存，避免将缓存误判为作品修改。

## 4.1.4 / 4.1.3

补充生成文本中的改口残留定位，包括破折号与省略号句式。只提醒，不阻断提交；允许经阅读确认的有意修辞。

## 4.1.2 / 4.1.1

增加情绪词重复与条款腔提醒，区分固定设定的核验用语和正文中的自然表达。提醒不自动改稿。

## 4.1.0

增加文学克制、网文爽文、言情细腻和类型紧凑四个文风档；与题材插件分别选择。按档区分题材语汇与空洞套话，结合本书认可样本检查文风偏移。

## 4.0.0

增加读者引擎卡、弧卡与悬念账；生成单位支持批次。批次须在同一会话连续写作，默认三章、最多五章，提交与回退按批处理。旧单章模式继续支持。

检查合并为批次连读与逐章复核；复盘记录下一段拟加强的内容或保留理由。读取包精简重复控制材料，限制 CONTROL 体积。空引擎卡不改变旧项目行为。

## 3.15.2 / 3.15.1

区分明确矛盾、证据不足和必需输入缺失；分页输出按实际范围报告。减少同次读取与无变化写入，保留正式提交的锁内重核。同步保留已有普通文件权限。

## 3.15.0

修复恢复、备份、路径链检查、同步保留策略、插件版本登记及候选授权边界。移动项目或缺少恢复输入时明确停止；已验证事务补完前核对提交关系。

## 3.14.0 / 3.13.0 / 3.12.0

完善连续阅读、阶段小结、作者交流、运行问题记录和旧问题回查。阅读范围与整体结论分开报告；运行日志不替代创作批准。保持同一候选和摘要的正式写入机制。

## 3.11.0 / 3.10.0

增加开书探索、材料导入、文风推荐与大纲论证。研究材料保存为 DATA；采用的约定进入原有开书或修订候选。完整研究不默认进入逐章读取包。

## 3.9.1 / 3.9.0

补充写作质量试验说明和空白记录模板，区分作者判断、真实读者反馈与软件验证。未进行外部试验不阻断正常写作。

## 3.8.0 / 3.7.0

改进创作意图传递、正文身份校验、修订回退和关联复核；体验呈现可作为章节内容，不强制每章制造转折。扩充相关反例测试。

## 3.6.1 / 3.6.0

修正重复动作清单与提交核对口径；增加公平推理检查、跨题材测试及技术文档。开书增加文风方向确认；字数目标可选，进度推算只提供参考。正式定稿不自动等于认可为文风样本。

历史版本仅允许规划批处理；4.0 起生成批次规则由 R85 取代。

## 3.5.0 / 3.4.0

将检查和写入纳入同一锁范围，异常路径释放锁；输出目录也进行链接边界检查。同步核对已安装插件与版本。题材手册按需读取，逐章规则随启用插件进入读取包。

## 3.3.0 / 3.2.0

增加开书生命周期、合作方式与 foundation 候选批准；增加文风基线、词表和定位提示。文风检查不自动改写，不以通用数值替代本书样本。

## 3.1.0 / 3.0.0

改用操作系统文件锁；恢复须显式执行。备份先校验后发布，默认拒绝符号链接；恢复只写入新目录。明确 CONTROL/DATA 信任边界、事务状态和回退语义。

## 2.0.1 / 2.0.0

建立 schema 2 项目结构、统一命令入口、声明式插件与多文件候选提交。后续版本保持结构兼容，通过共享文件清单升级。
