# 本项目自己积累的 AI 腔条目，一行一条，格式同 _工具/AI腔词表.txt。
#
# 母版那份收的是跨模型稳定的公共核。你实际用的那个模型还有自己的尾巴，
# 而且模型会更新——所以这份表由你在写作过程中积累，不随 sync 覆盖。
#
# 不用手写：跑
#     python3 novel.py style --suspect <草稿路径>
# 它会拿草稿和本书基线（正样本 + 已定稿正文）做词频对比，把"模型爱用、
# 你不爱用"的搭配列出来，你挑需要的加进这里。
