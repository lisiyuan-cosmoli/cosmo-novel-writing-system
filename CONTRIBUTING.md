# 参与贡献

欢迎报告可复现的问题、补充测试、改进文档与提交小范围修复。作者与维护者：LISIYUAN（[@lisiyuan-cosmoli](https://github.com/lisiyuan-cosmoli)）。讨论与代码提交使用 [cosmo-novel-writing-system 仓库](https://github.com/lisiyuan-cosmoli/cosmo-novel-writing-system)。

## 先保护作品与隐私

不要在 Issue、PR、截图、日志或测试夹具中上传真实小说、未公开设定、候选稿、读者资料、真实身份信息、密钥或令牌。用自行编写的合成案例复现；将本机用户名、绝对路径、书名和无关内容替换为占位符。不要直接附上整本项目、`.novel/`、备份或候选存根。

安全漏洞按 [SECURITY.md](SECURITY.md) 处理，不在公开问题中披露可利用细节。参与讨论须遵守 [行为规范](CODE_OF_CONDUCT.md)。

## 报告问题

先检索现有问题，再通过对应模板提交。请说明系统版本或提交号、操作系统、Python 版本、最少复现步骤、预期结果与实际结果。保留必要错误文本，并写明是否使用定制插件；工具体检通过与正文内容正确是两件事。

功能建议请描述具体使用场景、目前怎样完成、遇到什么困难，以及可验证的改善。涉及写入、批准或恢复流程时，说明对已有作品和旧项目的影响。较大的流程变更先提出方案，再开始实现。

## 修改与验证

从自己的分支提交 PR。每次尽量解决一个明确问题，保持 Python 3.9 兼容，核心运行不新增第三方依赖。修复行为问题时，补能复现该问题的合成反例；文档修改无需增加重复验证措辞的测试。

在母版仓库根目录运行：

```bash
python3 -B novel.py doctor --template
python3 -B novel.py release-test
```

开发过程中可先运行受影响的自测脚本，再做完整回归。在 PR 中写出实际执行的命令与结果；未执行或未通过时说明原因，不填“全部通过”。测试使用临时项目或隔离副本，不对真实作品试跑同步、提交、回退或恢复。

新增共享文件时核对 `system-manifest.json`：母版维护文件不应进入新书或同步清单；新增书籍运行文件应说明旧项目兼容方式。修改开始入口时检查首次登记、重复接续、已有资料保留与故障恢复，全部在临时目录验证。

提交前检查完整差异与新增文件，确认没有作品数据、私人路径、凭证、生成的读取包或运行记录。`.gitignore` 不能隐藏已经跟踪文件的改动，也不能代替人工核对。

## 许可与审阅

只提交你有权提供的内容，保留第三方材料的来源与许可。软件适用 [Apache-2.0](LICENSE)，文档与模板适用 [CC BY-SA 4.0](LICENSE-docs)，具体边界见 [NOTICE](NOTICE)；贡献应符合相应文件的许可。小说内容不会因使用本系统自动适用仓库许可。

维护者会根据复现证据、兼容性和维护成本决定是否合并，可能要求缩小范围或补充验证。提交建议或 PR 不代表已经获准改变创作流程，也不承诺处理时限。
