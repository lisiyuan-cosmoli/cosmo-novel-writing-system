# 规则由来

本文说明规则所防止的通用问题与实现位置。保留 R01—R98 编号供其他文档引用，不公开真实作品进度、使用者答复或个人操作记录。本文不进入逐章读取包。历史规则以本节替代说明、START_HERE.md 和执行契约为准。

### R01　新书必须登记独立项目身份

6.0 默认在下载的空白文件夹中通过 `novel.py start` 登记，已有项目只接续。`init` 保留作维护与兼容入口。身份损坏或已有作品但缺少身份时停止，不自动覆盖或重建。

实现位置：统一 CLI、开书入口、新建项目工具与 `project.json`。

### R02　共享文件只能按白名单同步

项目升级使用 novel.py sync。同步范围由发布清单决定，项目数据不参与覆盖。

实现位置：system-manifest.json 的 sync_files、同步试算和事务回滚。

### R03　插件自己声明安装文件

插件的安装、核验和卸载直接读取各自的 plugin.json，不由安装者手抄补丁清单。

实现位置：声明式插件清单、目标冲突拒绝、安装后按声明核验。

### R04　缺输入必须失败

必需文件、必需字段或可核对象取不到时停止，不把空结果解释为通过。

实现位置：doctor、brief、package 和 commit 的失败关闭策略。

### R05　章节变化先进入候选区

正式文件不直接改。全部变化先进入候选区，在影子项目试算，通过后再用事务提交。

实现位置：候选提交、共享排除表、正式复核与自动恢复。

### R06　多份状态必须作为一次提交

正文、大纲、快照、事实记录、流程审计和实际改动的插件表一起确认、一起提交。

实现位置：可恢复多文件事务和单次候选提交。

### R07　当前状态不能写成补丁堆

状态快照保存此刻成立的版本。被推翻的当前结论要替换干净，历史留在事务和归档。

实现位置：快照当前态、已推翻判断表、候选闸门与事务历史。

### R08　备份必须保留工具并排除运行垃圾

备份保存有效内容和工具，排除候选区、读取包、旧备份和事务运行历史。

实现位置：带摘要的压缩备份、统一排除表和只恢复到新目录。

### R09　一个文件就是一个读取单位

需要每章读取的当前事实与低频回查的旧事实分文件保存，不靠提示语要求模型只读大文件中的某一段。

实现位置：06_事实记录.md 与 06b_事实记录_已归档段.md 分离，正式读取包按文件装配。

### R10　维护记录不进入每章上下文

系统复盘、试跑耗时和维护说明与写作输入分开。

实现位置：流程审计、设计札记和本文独立保存，START_HERE 保持短入口。

### R11　简报与正式创作包分成两个阶段

先用简报准备章节卡，章节卡完成后再生成正式创作包。简报不能直接用于写正文。

实现位置：brief 与 package 两条独立命令及不同文件集合。

### R12　文档能力必须与工具实况一致

读取范围由工具和清单决定。未实现按行选择时，文档只能写整表读取。

实现位置：读取包直接按当前实现组装，并报告实际来源。

### R13　内部引用使用永久 ID

正文、章节卡、伏笔、事实和插件记录使用永久 ID。阅读顺序由大纲中的展示章号决定。

实现位置：四位永久 ID、大纲展示章号、迁移器和引用检查。

### R14　两栏用户决定不能由 AI 静默代填

章节卡里的具体信息与硬锚点由用户提供。AI 可以追问，只有用户明确授权时才代拟并留痕。

实现位置：章节卡锁定栏、代拟标记、选择理由和流程审计。

### R15　代拟需要公开出口

用户主动要求时可以获得互不重叠的选项，每个选项写清代价并记录选择。

实现位置：统一的代拟条件和流程审计。

### R16　多个走向不能由 AI 推荐

各走向只说明所得与代价，不标推荐。用户自己选择并记录理由。

实现位置：章节卡的走向选择和用户选择理由。

### R17　流程执行状态必须写进文件

每章记录实际完成、未触发和跳过的步骤，跳过要写理由。

实现位置：06_归档/流程审计.md 和候选提交。

### R18　完整模式与过渡模式并存

高影响章节使用完整模式，低影响章节可以使用事先定义的过渡模式。

实现位置：项目工作模式和章节卡的模式字段。

### R19　一章需要轻量场景清单

章节卡管理整章，每次地点、时间、目标或在场关系明显变化时，用一行场景清单展开。

实现位置：一张章节卡加一份轻量场景清单。

### R20　阻力必须计算实际代价

章节卡要求说明越过阻力会付出什么具体后果，不能只写一个听起来合理的障碍。

实现位置：本章变化中的阻力与具体代价。

### R21　篇幅是观测范围

先确认本章该完成的内容，再看篇幅。越出常用范围时记录原因，不为凑数字机械增删。

实现位置：项目读取上限、章节篇幅说明和人工质量判断分离。

### R22　开篇与收法需要跨章观察

写新章前查看近期章节形状，每五章并排检查开头和结尾。

实现位置：章节卡重复证据和四遍检查中的五章并排检查。

### R23　用户确认前必须看到正文

用户确认章节提交包前，要能直接阅读完整正文。路径、命令输出和 AI 的摘要都不能替代正文。

实现位置：每章交付规则和用户确认门。

### R24　冷读问题必须让普通读者听得懂

冷读使用简单问题，记录读者原话与行为，不要求读者诊断自己的走神位置。

实现位置：阅读驱动力记录中的低门槛冷读流程。

### R25　重复动作清单必须来自真的跑过一次检索

重复动作清单由 `novel.py repeat --card` 生成，并与正文人工核对。保留旧格式兼容；不要求使用者抄写命令输出来证明检查已执行。

实现位置：重复检查工具、章节卡和提交包校验。

### R26　会累积状态的物件要完整推演

设备、单据、记录本、系统、账户与耗材再次出现时，从上次出现开始列出所有接触、使用和状态变化。

实现位置：四遍检查第二遍的物件状态推演。

### R27　人物数过的东西要由编辑再数一次

正文出现总数、序号或“前几样”时，编辑者逐项数正文并与人物说法核对。

实现位置：四遍检查第一遍的人工计数。

### R28　叙述者判语要做删除测试

叙述者直接概括人物性格或人生时，删除该句，检查读者能否从动作得到同一印象。

实现位置：四遍检查第三遍的叙述者判语检查。

### R29　每五章要把结构并排看（历史节奏，现由三章/十二章复盘承接）

每五章集中查看开头、结尾、对白停顿和人物小动作。

实现位置：四遍检查中的每五章并排检查。

### R30　统一修改后必须检查连锁后果

四遍先诊断，再统一修改。修改完成后复查时间、知情范围、硬锚点、物件状态和新承诺。

实现位置：四遍检查的统一修改与复查清单。

### R31　推翻判断要登记并检索到零

先在已推翻判断表登记旧说法和检索词，再改当前文件，最后检索所有有效记录。

实现位置：已推翻判断表、候选闸门和当前态替换规则。

### R32　外部审查意见也要回到原文核验

第三方报告提供问题线索，不直接覆盖正文与账本。每条意见先找原文证据。

实现位置：迁移与修改流程中的证据优先原则。

### R33　读者会记住的内容必须有去向

阶段复盘要找出读者已经会记住、但尚未进入伏笔、事实或插件记录的显眼内容，并决定继续、淡化或删除。

实现位置：阅读反馈、事实记录和全书改稿阶段的未兑现内容检查。

### R34　检查器负责记账，不冒充内容编辑

机器检查永久 ID、文件、状态、引用、事务和可计算字段。动机、语言、阅读感与物理常识由人工复读。

实现位置：doctor 与 commit 的客观检查边界，以及四遍人工检查。

### R35　会误报的内容检查可能产生负收益

检查无法稳定区分正确与错误时降为人工提醒。持续误报就删除，不靠豁免维持。

实现位置：体检只保留客观形态与账本检查，正文检查交给四遍复读。

### R36　改过的检查必须先看到它失败

新检查或刚修过的检查需要一个必然报警的故障样本。确认它真的拦住错误后，绿灯才有意义。

实现位置：发布级自测中的故障注入与负样本。

### R37　闸门必须拒绝继续

阻塞错误返回失败，不能只报告黄灯后继续提交。

实现位置：命令退出码、候选试算和失败时不写正式文件。

### R38　全绿只表示可以继续

系统通过表示文件与记录满足已声明约束，不作为正文质量证明。

实现位置：验收报告、doctor 输出和每章交付说明中的人工边界。

### R39　没有错误证据时不扩大改动

结构看起来可以合并或简化时，先看它是否实际造成错误。没有证据就保持稳定。

实现位置：插件隔离、核心稳定和内容改动优先于无证据重构。

### R40　运行元数据损坏不得让诊断命令一起失效

事务锁零字节或内容损坏不影响操作系统 flock，常驻锁文件不删除。事务日志损坏时给出可操作的人工恢复步骤。`status`、`doctor`、`history` 三条只读命令在事务层异常时降级继续运行。

实现位置：`v2_core.load_json_lenient`、`事务._read_lock`、`事务._read_journal` 的可操作报错、`novel._project(只读=True)`，以及发布自测两条反向样本。

### R41　用名单匹配模板的检查，必须有咬住真模板的样本

开书初始化检查按结构取表格第一行做表头，不按名字排除。所有拿名单匹配模板的检查都要有一条直接读现行母版模板的失败样本。

实现位置：`读取包.初始化缺项` 按结构取表头，夹具表头同步更新，新增样本㉝㉞ 直接读现行模板。

### R42　承重的手工流程必须有命令、有提醒、有检查

封段做成 `novel.py seal` 命令。`status` 与 `package` 在事实记录超过目标线三成、成品包到达硬上限八成五时提前提醒。摘要仍由人写，工具只做搬运。

实现位置：`_工具/封段.py`、`_工具/状态.py`、读取包带宽预警，以及五条封段样本。

### R43　编号不能写死位数

伏笔与线索编号按 `F`／`C` 加两位以上数字匹配，先定位编号再看同行窗口，位数没有上限。

实现位置：`提交包.编号位置` 与 `提交包.同行窗口`，以及三位数编号的失败样本。

### R44　排序键必须唯一

事务号带微秒。历史与"哪一笔生效"的计算以清单里的 `created_at` 为主键。

实现位置：`事务.apply_changes` 的事务号格式与 `事务.ordered_commits`。

### R45　撤销操作要说清它自己也是一笔提交

`rollback` 输出明说连按两次是重做。`history` 标出 `← HEAD` 与"已被撤销"。

实现位置：`事务.undone_ids`、`novel.py` 的 history 与 rollback 输出。

### R46　检查名要写"通过时那句话"

体检的告警型检查为"有问题"和"没问题"各写一个名字。

实现位置：`体检.warn` 的双名签名与代理执行协议的报告口径。

### R47　信任边界按"要不要照着做"划分

已启用插件的逐章检查与 `项目配置.md §一–§六` 标为 CONTROL。DATA 可以作为写作约束，其中的指令式句子不是工具指令。任何一侧都不能扩大写入范围。

实现位置：`读取包.组装` 的分类函数、包头措辞、代理执行协议第二节，以及信任边界样本。

### R48　提示给出的命令要能直接跑

工具输出的命令不留字面占位符。

实现位置：`落盘._建议命令`。

### R49　测试用的破坏开关必须有门禁

故障注入需要同时设置 `NOVEL_SELFTEST_FAULTS=1` 才生效。

实现位置：`事务._faults_enabled` 与三处调用点，以及"未打开开关时不生效"的样本。

### R50　互斥必须由操作系统持有

事务互斥用 `fcntl.flock`。锁文件常驻，内容只作诊断；删除再建会绕过原 inode 上的锁，不得删除。锁句柄贯穿整个事务生命周期。

实现位置：`事务._acquire` / `_release`、50 轮并发样本、强杀释放样本、异常锁不死锁样本。

### R51　恢复是写入，写入不能隐式发生

`inspect()` 只读探查，`recover()` 显式恢复。只读命令报告状态，写命令拒绝并指向 `recover`。

实现位置：`事务.inspect` / `_pending_guard` / `recover`、`novel._project(写入=…)`、只读组前后哈希比对样本。

### R52　同一件事的写入端与校验端必须共用一套策略

备份的创建器与校验器对符号链接采用同一套默认拒绝策略。临时包先跑完整 `verify`，通过后才原子改名。

实现位置：`备份.scan` / `create`、四类链接拒绝样本、失败无残留样本。

### R53　用公共前缀判断"本轮是否干净"必然误报

每轮自测建自己的顶层沙盘，只检查和清理本轮登记的资源。禁止扫描全局前缀。

实现位置：四份自测的本轮沙盘与登记表、外部同名前缀样本、两进程并行样本。

### R54　契约有意变更时，重写断言并写明新旧契约

修改会改变外部契约的实现时，必须列出哪些断言因此失效、新旧契约各是什么，并把说明写进测试本身。禁止改名或放宽了事。

实现位置：`发布自测.事务元数据健壮性` 的 docstring 与 CHANGELOG 的契约变更表。

### R55　基线由作品自己产生，不由母版规定

文风基线从本书的正样本、加上用户在 `00_设定层/02_风格样本.md` 里明确认可的已定稿章节算出，写进 `02_检查层/11_文风基线.md`。母版不预设任何分布标准。（3.6 起已定稿不再自动进语料，理由见 R72 自产语料不能当标准。）

### R56　样本不足时不给数字

正样本与已认可章节合并不足 2000 字时，**不出分布层基线**，只报指纹命中，并说明原因。已定稿章不足 3 章时偏离按百分比报告，够 3 章后换成逐章标准差倍数。

### R57　只定位，不打分，不改写

文风检查不输出总分，不作为提交闸门，也不自动改写正文。它只给命中行号、分布偏离和一段定点改写提示词。

### R58　指纹表是缓存，发现机制才是通用的

`_工具/AI腔词表.txt` 只收跨模型稳定的公共核，随 sync 更新。各模型自己的尾巴由 `style --suspect` 从实际稿件里发现，积累进 `AI腔词表_本项目.txt`。豁免写在 `允许AI腔.txt` 并注明理由。

### R59　字段非空不能代替用户确认

开书状态存在 `project.json` 的 `initialization.status`，只有 INIT 正式事务能写入 `confirmed`。字段填满不会让状态自动变化。

实现位置：`foundation` 命令、`foundation-policy.json`、读取包的开书闸门、新书自测那条「字段全部填满但开书未确认时仍然拒绝出简报」。

### R60　题材选择不是授权，沉默不是批准

题材只承担分类与插件建议。合作方式单独确认，四种取值写进创作意图。跳过回复、只回"可以"、没有反对，都不算确认。

实现位置：`00_创作意图.md` 的合作方式栏、开书闸门的模式规则、代理执行协议第三节。

### R61　开书与章节用同一套机制

开书走候选区、试算、完整摘要批准、可恢复事务，与章节提交完全同构。`foundation-policy.json` 与 `chapter-policy.json` 结构一致。

### R62　迁移不替用户签字

`sync` 给旧项目补开书状态时，有正文的标 `legacy`，空的标 `draft`，**绝不标 `confirmed`**。`legacy` 可以继续写。

### R63　指导要放在会被读到的地方

题材内容分两层：每章必须执行的检查蒸馏进 `02_检查层/插件/<id>.md`（作为 CONTROL 进读取包，每份不超过 1200 字符），判断依据留在 `04_题材插件/<题材>.md`（不进包，`novel.py guide` 按需调阅）。检查文件末尾指向对应手册。

实现位置：`_工具/题材手册.py`、检查文件的字符上限与母版检查、四份手册末尾的全书改稿追加项。

### R64　带上限的东西，上限要有人守

进读取包的插件检查文件不超过 1200 字符，由母版检查强制，并配一条"撑过上限就报错"的失败样本。

### R65　检查与写入必须在同一把锁内

需要"读当前状态 → 校验 → 决定写什么"的调用方必须用 `事务.apply_changes(plan=...)`，计划函数在锁内执行。上层不得先检查后加锁。

### R66　取锁之后的每一行都要被 finally 覆盖

`_acquire` 之后立刻进入 `try/finally`，锁内的预检、目录创建、快照和日志写入全在保护范围内。

### R67　被排除表跳过的目录，同样是攻击面

所有写入入口共用 `v2_core.safe_output_dir`：逐级 `lstat`，拒绝符号链接，要求最终真实路径落在项目内。

### R68　升级要能证明自己生效了

插件文件标 `owner`，安装时记录托管文件哈希与版本；`sync` 更新托管文件、归档被改过的旧版；`verify` 同时核对版本。

### R69　勾选框不能覆盖自由文本里的待确认

「待确认」标记（待用户确认、待定、待改选……）是机器判据。草稿阶段提醒，定稿阶段阻断。复选框不参与这个判断。

### R70　推断落在事实行，后面每章都会把它当前提

`06_事实记录.md` 的「正文事实」行不得出现推断词。判断归「角色判断」行，并写明是谁的判断。

### R71　机器抓不到的，不要假装能抓

世界规则冲突只做字面提醒。真正的逐条核对写进 08 第一遍，由人执行。`已知限制.md` 明写这条限制。

### R72　自产语料不能当标准

文风基线只用正样本与用户明确认可的章节。已定稿不自动进基线。

### R73　正样本必须先于正文，且不能由被衡量者自己写

文风方向是开书第十项必须确认的决定。正样本来自四条路径之一，当前开书门槛为 ≥800 字、分析 ≥5 项；3.15 起硬规则按作者实际约定填写，不设数量下限。工具出提示词、核对、写入，不生成文字也不替人选。

### R74　候选的价值在于它们真的分开

`voice --prompt` 给的是五个维度上对立的取值，要求三段各取不同的一端、写同一场景、不用第一章开头。`guided` 下只给一个候选会被拒绝。

### R75　只在一个题材上验过，等于没验过通用性

发布自测包含跨题材组：言情、科幻、不装插件的历史各建一本完整项目，走完开书到体检的整条流程，并逐项核对**未启用插件的字段没有泄漏**。夹具必须与任何实跑作品无关。

同时静态扫描核心模板与检查文档，防止题材专属必填项进入核心。隐私检查使用外部维护的私有专名清单扫描公开候选，清单不能随母版发布；发布自测仅用随机合成标记验证扫描器的正反例，并覆盖复盘文档与测试源码，不冒充真实作品隐私已获保证。

实现位置：发布自测的 `跨题材组`、`章节卡题材中立组`，以及三本与实跑作品无关的夹具。

### R76　规划与生成的批次边界（历史规则，生成部分由 R85 取代）

3.x 将规划批处理与单章生成分开。4.0 起正式读取包允许不超过 `batch_max` 的批次，要求在同一会话中按顺序连续写作，让后章依据前章已写出的正文；批末统一回填账本。跨会话时仍须读取正式状态。

章节卡时间提醒只能提示可能过期，不能替代内容复核。实现位置：读取包、章节卡与发布自测。

### R77　揭晓之前必须已经铺过，且铺垫要在更早的一章

悬疑插件启用时，已登记的揭晓必须有更早且实际存在的首现章节；未在对应事实记录登记时提醒。机器核验章号和文件，读者能否据此推理仍须人工判断。不增加字段，未启用插件时不触发。

实现位置：线索兑现表与悬疑插件提交检查。

### R78　开书承诺必须能抵达下一次生成

已确认的创作意图必须进入规划简报和正式读取包，并保持 DATA 身份。回归应修改相应字段并比较生成结果，不能只检查文件存在。

### R79　撤销必须对应一笔实际登记的修订

普通编辑不会登记为可回退事务，备份和体检也不代为登记。全局修改应通过 revise 及独立内容白名单提交；摘要绑定候选、有效内容和 HEAD，锁内重新验证。直接编辑后的恢复需要备份或人工比对。

### R80　减负不靠补写虚假的选择

已有明确且获确认的路线可以继续沿用，不为填表虚构多个走向。来源与授权如实记录；机器不能证明用户实际阅读或确认。长期保留事实须明确标记，未标记旧事实按需调阅。

### R81　正文身份约束不能只在某个入口存在

正文文件头的永久 ID 应与文件名及大纲一致。体检、章节提交与修订复用身份检查；核对旧章不要求把当前快照退回历史状态。

### R82　修改正文以后，关联记录的核对结论需要重新取得

修改正文后需重新核对受影响设定、账本及后续正文。核对范围、说明、候选与有效基线绑定，变化后原批准失效。记录可追溯不等于自动语义验证。

### R83　体验呈现不必伪装成一次情节转折

3.7 的过渡模式仍强制填写阻力、代价和不可逆变化。3.8 把章节重心和工作模式分开，让观察、回忆和日常相处按具体体验获得阅读价值。保留事实与连续性检查，并给授权试写留出正式账本之外的临时空间。

### R84　读者引擎卡放在读取包的最前面

每本书一张 `00_设定层/04_读者引擎.md`：读者承诺、核心爽感单元、升级阶梯、对手、长线问题、类型套路、强度边界。填好后引擎层启用，它与当前弧卡、悬念账排在每一份读取包的最前面。它是 DATA，不是 CONTROL。

实现位置：`引擎.py`、`读取包.py` 的装配顺序；新书开书第十一项决定 D11。

### R85　生成以批为单位，同一会话连续写，一次提交

`package` 与 `commit` 接受 `K0019-K0021` 这样的批次，章数不超过 `project.json` 的 `batch_max`（缺省 3，硬上限 5）。批内章节在同一会话里按顺序写，每章先存进批次候选；两遍检查后统一回填账本，一个摘要、一笔事务提交，`rollback` 整批撤销。第 1—3 章、弧末高潮章和作者指定章仍一章一批。

实现位置：`落盘.py` 批次候选与逐章闸门；`提交包.py --批末` 快照口径；`重复.py` 取样批内草稿。取代 R76 的生成部分。

### R86　规划以弧为单位，作者批准弧卡

一弧 5—10 章。弧卡写弧目标、对手、中点、低谷、弧末高潮与每章三拍，经 revise 事务由作者批准。弧内章节卡沿用弧卡，不再逐章比较三个走向；出现新分歧时写「本章比较」并给两个走向。

实现位置：`01_运行层/07b_弧卡_模板.md`、`arc --new`、`revision-policy.json` 的弧卡路径；R16 的执行位置从逐章移到弧卡。

### R87　章末钩子必须是已经发生的事实或决定

弧卡范围内的每一章在悬念账登记一条章末钩子。它必须是正文里已经发生、撤不回的事；类型限七类，弧末收束只用于弧的最后一章，其他章要安静收尾须写明理由。提交闸门核对这一行。

实现位置：`提交包.py` 钩子检查；网文插件第二条升为核心规则。

### R88　悬念账常驻 3—5 个问题

每章至少开一个或推进一个问题，每 2—3 章兑现一个；解 1 增 2 与超过 8 章未推进是警报。

实现位置：`01_运行层/05f_悬念账.md`、`hooks`、体检与 status 的提醒。

### R89　复盘必须落到下一段的加强措施

复盘记录的「下一段加强措施」写出至少一项具体安排，或写「保留：理由」。可以不回改旧章，但必须调整接下来的计划。修订 3.14“观察不自动产生改稿任务”：观察仍不自动改旧章，但会改计划。

实现位置：`结构复盘.py` 字段核对与复盘模板。

### R90　写进引擎卡的类型套路不算 AI 腔

去 AI 感只管语言层，不管情节层。引擎卡登记的类型套路不因“常见”被撤下，新鲜感落在细节上。修订 3.12 防同质化的误用。

实现位置：读者引擎卡第六节；网文插件类型引擎库；两遍检查第一遍。

### R91　新增规则先问：它会不会让故事更平

新增或放宽任何流程规则前回答这一问。答案为会时，必须配一条对应的“要”。每次发布统计读取包里的 CONTROL 字符，超过 5,000 不发布。

实现位置：本档案“使用这份档案”的三问之外新增第四问；08 检查、执行契约与代理协议的包内版本各自限长。

### R92　文风先选档，再用样本微调

新书开书用 `voice --profile` 明确选择文学克制、网文爽文、言情细腻或类型紧凑之一；所选档的写作约束与第二遍语言检查作为 CONTROL 进包。旧书缺省按文学克制处理。

实现位置：`04_题材插件/文风档/`、`文风档.py`、`读取包.py`、`开书.py`。

### R93　去 AI 腔是语言层的，要按档区分题材语汇与空洞套话

母版通用层词表按所选档放行类别。网文档不拦动作、情绪与节奏调度类题材语汇，只拦氛围套路与叙述者空泛收束；文学档全拦。本书仍可用 `允许AI腔.txt` 与本项目词表微调。

实现位置：`文风.载入词表`；文风档组的双向样本（网文档放行、文学档照拦）。

### R94　选定的声音要有东西守

外放档的正样本建议 3,000 字，开书即可建立文风基线；`style` 对照正样本统计情绪直写、内心独白、旁人反应，本章低于样本一半时提醒“正在往克制漂”。只提醒，不拦提交。

实现位置：`文风档.表现度量`、`文风档.漂移提醒`、`定声.py` 扩维与样本建议。

### R95　设定写成条款，正文要说人话

固定设定里的规则可以按条款写，用来堵漏洞、方便核对；系统提示、对话和叙述用人物日常的说法。`style` 对【】系统提示和“”对话里的条款限定词（真实、结算、对应、本人、用于、尚未……）提醒；对话只查公文味最重的几个词。第二遍语言检查问“现实里会有人这么说吗”。只提醒，不拦提交。

实现位置：`文风.条款腔`、`文风.报告条款腔`；08 第 6 项；网文档语言检查；固定设定模板“正文里怎么叫”一栏；文风档组条款腔 3 项。

### R96　提醒要成对：往克制漂的反方向是套话

外放档的 `style` 除了“往克制漂”，再对同一个情绪词在一章里用到第二次、或连着三章都在用的情况提醒，并提示用动作、话语、具体后果换着说，不补同一批计数词。两条提醒同时只提醒，不拦提交。

实现位置：`文风档.重复提醒`、`文风.前两章正文`；文风档组 4.1.2 三项。

### R97　代理的改口要能被机器定位

指纹表“改口残留”一类定位两种句式：动作字后紧跟破折号，以及破折号前后重复同一截。合成例子分别为「他推——他把空纸盒移到了墙边」与「门外传来一阵响——传来一阵轻响」。只定位，不拦提交。

实现位置：`_工具/AI腔词表.txt`「改口残留」；文风档组 4.1.3 一项。

### R98　换了标点的改口也要能定位

指纹表“改口残留”追加两种句式：省略号或破折号后接“不，”，以及动作字后紧跟省略号。合成例子：「他正要敲门……不，只是用指节碰了碰门板。」只定位，不拦提交。

实现位置：`_工具/AI腔词表.txt`「改口残留」；文风档组 4.1.4 一项。

## 3.15　全方位评估后的修正接续

本节说明旧规则编号在后续版本中的实现变化。

- **R51、R66：恢复必须完整。** 旧实现用日志绝对目录找 before，移动项目或缺快照后可能跳过恢复却删日志报成功。现在按当前项目和 ID 定位，先校验全部输入，再恢复并逐文件核验；日志在成功后才清理。新记录增加 before 哈希；旧日志无哈希时无法追溯认证快照来源。已验证事务补完前核对 HEAD 没有偏离父提交或本事务，晚期失败可以重试。
- **R67：排除目录也受边界保护。** 事务元数据与锁、恢复目标、开书/章节候选父目录均可能藏有链接；旧末级检查挡不住外部写入和清理。当前复用完整路径链检查，init 用真实父路径保护母版隔离。检查不构成对任意恶意竞态的证明。
- **R02：保留选项应与实际写入一致。** 旧 sync 接受混合文件 keep-local 却仍合并，倒置标记又会生成空区块。当前明确保留整文件，实际合并前核验唯一、成对且有序标记；损坏内容不会当成空白。完整空区块与目标缺失继续支持。
- **R68：安装版本独立于检查文件字节。** 只更新插件包而检查正文不变时，旧逻辑漏更新登记版本。当前按最终包和保留策略登记并复核，定制包与用户文件不用于强行对齐母版。
- **R52：恢复的内容必须等于验证的内容。** 旧校验忽略清单外的 FIFO 等特殊成员；目标磁盘大小写别名还可能把两个归档文件合成一个。当前校验普通类型、唯一规范路径和祖先冲突，恢复在临时目录拒绝文件系统别名碰撞，再比对全部恢复字节后发布。
- **R15、R59—R61：记录单项真实委托。** author_led 默认不代拟，但作者明确委托某项时，可记录 AI 代拟且用户批准及其范围、理由；不能为过闸门把来源改成用户原案。所有模式按相同来源判据核理由；已有方向的 D10 单项委托有单候选出口。旧确认项目不重新开书。
- **R73、R80：样本约定与模板一致。** 旧模板允许无硬规则，开书却强制三条。当前保留样本及分析要求，硬规则按真实需要填写；取消数量门槛不代表样本足以保证长篇声音，也不应将一次偏好推成通用禁令。
- **R17、R34、R35、R12：流程陈述应有清楚含义。** 审计七步现定义为提交前细目，后续试算、作者批准及正式提交另由真实输出与事务记录证明。判断次数按整体诊断和取舍轮次计，不按工具调用凑数；历史行保留，展示正文不冒充用户读过。

相关反例纳入发布自测；软件检查不证明文学效果。
## 使用这份档案

修改规则前，确认旧问题是否已由其他机制覆盖、新方案是否有失败反例，以及失败时如何保护正式资料。新增限制还需检查它对创作的影响和读取预算。技术规则不代替作者判断。

## 6.0 模式与旧规则的适用范围

R84—R90 中的升级、对手、固定回报和钩子要求须结合当前创作模式解释。通用模式围绕阅读期待与阶段发展，允许关系、认识和体验推进，不强制制造对抗或危机。升级对抗模式按明确选定的承诺核对。未填写模式的旧卡保留兼容行为，具体执行以现行引擎卡、执行契约及工具为准。
