# 06b 已归档事实段

> 本文件不进入日常读取包。它保存已经压缩成阶段摘要的章节事实。
>
> 需要回查旧细节时，按永久 ID 或标签读取对应条目。

## 归档内容

（第一次封段时由 `python3 novel.py seal <起> <止> --apply` 自动写入。不要手工搬运——手工搬容易漏条目，也不会留下事务记录。）

每一段以 `## 记忆段 K#### 至 K####` 开头，下面是该段的原始章节条目，逐字保留。
