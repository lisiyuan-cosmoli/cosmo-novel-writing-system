# 旧项目迁移说明

## 5.0 及其他 schema 2 项目 → 6.0

项目结构仍为 schema 2。使用新版母版的 `sync <项目> --check` 查看差异，在该项目已授权范围内同步，不重新开书，不运行 migrate。先保存完整可恢复副本；使用新版备份工具时，留意普通备份不含候选、候选存根与事务历史。

- 发布清单保留原有基线，并加入升级前真实 5.0 共享文件哈希。已登记的安装哈希优先；未知本地修改继续报告冲突。
- 6.0 更新工具、检查和说明，已有正文、设定、引擎卡、弧卡、作者记录、候选与识别到的定制插件保持原字节。旧引擎卡没有模式字段时沿用原升级规则，不自动改成通用模式。
- 新书引擎模板默认通用。旧书希望换模式时，在原引擎卡的修订候选明确选择并补足对应内容，经同一修订摘要批准；不为升级改写已确认路线。
- 当前文风导入会进入 INIT／REVISE 候选，`voice --check` 标明检查来源。已经存在的候选不因系统同步自动提交或取得新批准。
- 升级后重新运行 status／doctor，生成正式读取包；修改过的工具、候选或基线应重新试算，不复用过期摘要。历史回退仍是“再撤销上一次提交”，6.0 修正连续多次回退的生效标注。
- 新用户直接打开下载的文件夹并发送开书提示词，旧的双击快捷脚本已撤下。已有作品继续使用原目录，不重新下载覆盖作品；系统升级与创作修改分开执行。

## 4.2 → 5.0

schema 仍为 2，使用 sync。新增人物声音表为空模板种子；已存在不覆盖，空模板不启用声音层。已填人物与章节内容不因升级自动改变。

## 4.1.3 → 4.1.4

schema 仍为 2。`sync` 只更新 `_工具/AI腔词表.txt` 与说明文件。已写章节若新命中省略号式改口，改不改由作者决定。

## 4.1.2 → 4.1.3

schema 仍为 2。`sync` 只更新 `_工具/AI腔词表.txt` 与说明文件。所有文风档都会检查改口残留；已写章节若命中，改不改由作者决定。

## 4.1.1 → 4.1.2

schema 仍为 2。`sync --check` 审阅后 `sync`，只更新 `_工具/文风.py`、`_工具/文风档.py` 与说明文件，不改本书内容。已写章节同样会收到情绪词重复提醒，改不改由作者决定。

## 4.1.0 → 4.1.1

schema 仍为 2。`sync --check` 审阅差异后 `sync`：更新 `_工具/文风.py`、08 检查提示与网文档。已开书项目的 `01_固定设定.md` 属于本书内容，不会被改；想用“正文里怎么叫”一栏，走 revise 自己加一行。已写章节不自动改写，`style` 对旧章节同样会给条款腔提醒，改不改由作者决定。

## 4.0.x → 4.1.0

schema 仍为 2。`sync --check` 审阅差异后 `sync`：会补入 `04_题材插件/文风档/` 与 `_工具/文风档.py`，并更新 `11_文风基线.md` 的通用约束一节（本书基线区块保留）。旧书不选档时按文学克制处理，写法与之前完全一样。要换档：`python3 novel.py voice --profile webnovel`，建议同时把正样本扩到 3,000 字。已写章节不自动改写。

## 3.15.x → 4.0.0

schema 仍为 2，不需要 migrate，也不重新开书。按下面顺序做：

1. 先 `python3 novel.py backup "/项目" --note "升级 4.0 前"`
2. 从新版母版运行 `python3 novel.py sync "/项目" --check`，审阅差异。本版有 3.15.2 共享文件基线；项目已登记的安装哈希仍优先
3. 确认后 `python3 novel.py sync "/项目"`。它会更新工具与说明，补入缺失的 `00_设定层/04_读者引擎.md`、`01_运行层/05f_悬念账.md` 两份空模板（已存在不动），并补入弧卡模板与 `_工具/引擎.py`
4. 此时引擎层**尚未启用**，旧流程照常可写，旧章节卡照常通过

要启用引擎层：

5. `python3 novel.py engine --prepare` 把引擎卡、悬念账和原大纲放进修订候选
6. 和作者一起填引擎卡（读者承诺、爽感单元、升级阶梯、对手……），在悬念账登记当前挂着的问题
7. `python3 novel.py arc --new A02 K0019-K0027` 为下一弧建弧卡；在原大纲登记规划行
8. `revise --review`、`revise` 试算，作者批准后落盘。之后 `engine` 应显示“已启用”，下一批可以 `package K0019-K0021`

已写的章节不回改，也不要求补登悬念账：钩子检查只作用于弧卡范围内的新章节。原先写在大纲或结构复盘里的引擎、弧卡与悬念账草稿，迁到这三份正式文件后，大纲里只留引用，避免两处真相。

启用网文插件的项目 sync 后，插件逐章检查更新到 4.0.0；用户拥有的 `05e_钩子与爽点表.md` 不动。4.0 起新章节的钩子登记在核心悬念账，05e 只记阅读回报。

## 3.15.2 的同步范围

3.15.0 / 3.15.1 的 schema 2 项目可从新版母版先执行 `sync <项目> --check`，审阅具体差异、定制内容和备份后按授权同步，不重新开书。本版加入来自真实 3.15.1 母版备份的共享文件基线，仍优先使用项目已登记的安装哈希。

更新四遍检查的证据分类与分页阅读提示；已有作者记录、已填复盘、正文、候选和定制插件保留。旧记录中未收尾的“待批准”或旧审查周期需按当前证据另行处理，系统同步不会代作者修改这些资料，也不能据旧记录再次推断授权。

同步后重新核对读取包、候选依据和实际阅读覆盖。到达分页末尾不证明前页已输出或读完。本版不改变外层三个脚本。

## 3.15.1 的同步范围

本版继续使用 schema 2。已有 3.15.0 项目从新版母版先运行 `sync <项目> --check`，按实际差异及授权同步，不需要重新开书或 migrate。发布清单加入来自真实 3.15.0 母版备份的共享文件基线，原有历史基线不改写；仍优先使用项目已登记的安装哈希保护定制。

同稿交付、复盘自由文字和读取包展示发生精简，原结构复盘标题与机器标记保留。已填复盘、审计、作者答复和作品资料不覆盖、不补造。新书审计模板明确宿主必填、其余追踪列按需；旧书现有审计表仍沿用原解析兼容。

修订批准继续绑定全部候选及完整有效基线，实际事务只写变化文件及需刷新时间的图谱。升级后须重新生成读取包和候选依据，旧批准不能用于变化后的候选。更新共享文件保留目标已有权限，已损坏的历史权限需要根据该项目备份另行核对，不盲目统一权限。

外层断点接续脚本属于母版外的独立入口，不随作品 sync 复制。普通接续读取近期及相关范围，明确要求完整接手或使用 `--full` 时仍通读；不改变只交接、不自动创作的入口边界。既有作品不因母版发布自动迁移。

迁移器处理原 Markdown 结构项目，并保留小说正文与项目数据。

## 迁移前

先复制一份旧项目到安全位置。旧快照可能很大，复制前可以查看目录占用。

迁移默认只显示计划。

```text
python3 novel.py migrate "/旧项目路径"
```

计划会显示推断出的插件、归档基线、章卡改名和共享文件更新。

## 执行迁移

```text
python3 novel.py migrate "/旧项目路径" --apply
```

迁移会完成这些工作。

- 建立 `project.json`
- 写入结构版本与插件决定
- 更新通用工具和说明
- 补齐缺失的核心文件
- 把旧的 `场景卡_KNNNN.md` 统一为 `章节卡_KNNNN.md`
- 在需要时补入快照版本抬头
- 建立可恢复事务记录

迁移不会把其他题材的规则写进核心，也不会把案例数据带进母版。

## 迁移后

```text
python3 novel.py doctor "/旧项目路径"
python3 novel.py plugin list "/旧项目路径"
python3 novel.py backup "/旧项目路径" --note "迁移完成"
```

旧项目若存在同名章卡且内容不同，迁移会停止，留给项目单独裁定。

## 从 2.x / 3.0–3.5 升级到 3.6

项目结构版本没有变，仍是 schema 2。**已经是 v2 的项目不需要重新迁移，同步即可。**

```text
python3 novel.py sync "/项目路径" --check
python3 novel.py sync "/项目路径"
```

同步会带来这些内容，并把 `project.json` 的 `system_version` 更新到 3.6.1。

- 修好的读取包（状态快照 C／D／E 三节的初始化检查恢复生效）
- 修好的提交闸门（伏笔与线索编号不再限两位）
- 修好的事务层（零字节锁不再锁死项目，事务号带微秒）
- 新增的 `seal` 封段命令与 `status` 一屏状态
- 恢复成可执行提示词形态的 `08_四遍检查提示词.md`
- 新的 CONTROL／DATA 信任边界与代理执行协议
- 换成操作系统文件锁的事务互斥（3.1）
- 不再隐式恢复：只读命令零写入，恢复只由显式 `recover` 触发（3.1）
- 备份对符号链接默认拒绝，且先自校验再改名（3.1）
- 文风度量 `style` 与生成期文风约束 `11_文风基线.md`（3.2）
- 开书孵化 `foundation` 与 `initialization` 开书状态（3.3）
- 四份题材手册与 `novel.py guide`（3.4）
- 五项数据安全修复与插件真升级（3.5）
- 待确认门禁、事实推断分行、读取包裁剪、文风语料授权、字数预测（3.6）

### 3.6 升级之后要做三件事

**一、把文风基线重建一次。** 3.6 起已定稿正文不再自动进基线——模型写的正文默认不是文风样本。升级后 `style --baseline` 会告诉你有多少章"未被认可"。

你读过、认为"这就是这本书该有的声音"的章节，写进 `00_设定层/02_风格样本.md` 新增的这一行：

```
| 认可为文风样本的章节 | K0003、K0005 |
```

然后重跑 `style --baseline --apply`。**旧基线不会被自动删掉**，但它是从未经认可的语料算出来的，认可清单定好之前建议先不看分布层。

**二、下一章开始用 `repeat` 命令。** 章节卡的重复动作清单不用手抄了：

```text
python3 novel.py repeat K0008 --card
```

把它输出的五行**整块**替换掉章节卡「重复动作清单」的表体。重复证据状态也在这一块里——3.6.1 起它不再同时挂在「连续性约束」下，不用填两遍。

**已经写好的旧卡不用改**——校验器同时认旧式（`### 取样范围` 下手抄 grep，状态写在连续性约束里）和新式（结论块整块）两种写法，两处都有也认，新式优先。

**三、检查有没有"待确认"还打了勾的章卡。** 3.6 起这会在定稿时被拦。已经定稿的旧章不受影响，但下一章开始要么让用户确认，要么把那句话改掉。

### 3.6 会拦住你以前能过的东西

| 情况 | 3.5 | 3.6 |
|---|---|---|
| 章卡里写着待确认、复选框打勾 | 通过 | 草稿阶段提醒，**定稿阶段拦住** |
| 「正文事实」行里写"推想""坐实" | 通过 | **拦住**，要求移进「角色判断」行 |

这两条都是有意收紧。如果你的下一章被拦住，先看它指的那一行——它指的位置都是具体的。

### 3.5 升级会多做一件事：真的更新已安装插件

> 以下是 3.5 当时的历史策略。当前版本遇到本地修改或未知基线先报冲突，按明确的 keep-local / replace-local 处理；不可依据下面的旧说明直接覆盖。

3.4 及更早的 `sync` 不碰项目里装好的 `02_检查层/插件/*.md`，所以你的项目很可能停在装插件那天的版本。3.5 的 `sync` 会更新它们。

因为 3.5 之前没有安装记录，工具**判不出你改没改过**，所以一律先把旧版归档到 `06_归档/插件/同步归档/<时间戳>/` 再更新。多一份归档，不会丢东西。

先看计划再决定：

```text
python3 novel.py sync "/项目路径" --check
```

计划里 `归档并升级` 的行就是这一类。确认之后正式同步。

### 3.5 的审计表多了三列

`生成平台` 拆成 `宿主平台 / 模型提供方 / 模型名称 / 读取包字符数`。**旧项目已经写好的审计行不用改**——提交闸门同时认新旧两种表头。下一章开始按新列填就行。

### 3.4 升级之后

四份题材手册（`04_题材插件/<题材>.md`）随 sync 更新——它们是母版文档，不是项目数据。

已经装了插件的项目，`02_检查层/插件/<id>.md` **不会**被 sync 覆盖（那是项目数据）。想拿到新版逐章检查，卸载再装一次：

```text
python3 novel.py plugin uninstall suspense --force
python3 novel.py plugin install suspense
```

`--force` 会先把你已经填过的内容归档到 `06_归档/插件/`，不会丢。

### 3.3 升级之后：你的项目会变成 legacy 还是 draft

同步时会按**实际内容**给项目补一个开书状态，绝不静默标成 `confirmed`。

| 项目现状 | 补成 | 影响 |
|---|---|---|
| 已有已定稿、初稿或待返工章节 | `legacy` | **可以继续写**，只在出包时给一条提示 |
| 空项目，一章都没有 | `draft` | 要先走 `foundation` 才能写第一章 |

判断"已经进入创作阶段"时初稿与待返工同样算数——只看已定稿会把一本正在写的书突然拦住。

`legacy` 项目想补做开书确认，随时可以：

```text
python3 novel.py foundation --prepare
```

`00_设定层/00_创作意图.md` 与 `06_归档/开书决策记录.md` 属于用户数据，通过种子机制**只在缺失时补入空模板**，不覆盖你已经写的内容。

### 3.2 升级之后要做一件事

`11_文风基线.md` 由 sync 补入，但**基线本身是空的**——它必须从你这本书自己的语料算出来。升级后跑一次：

```text
python3 novel.py style --baseline --apply
```

已定稿章够多时它会直接建立；语料不足两千字会明说，不给不可靠的数字。`status` 与 `doctor` 会一直提醒到建立为止。

`_工具/AI腔词表_本项目.txt` 与 `允许AI腔.txt` 是项目数据，sync 只在缺失时补空模板，不覆盖你已经积累的内容。

小说正文、设定、状态快照、事实记录、伏笔表、流程审计、项目配置和已经填写的插件数据都不在同步覆盖清单内。

### 同步之后立刻做两件事

1. 运行 `python3 novel.py status`。v2 项目的状态快照 C 知情范围、D 立场与筹码、E 场上的物件三节，此前**从未被真正检查过**。如果这本书是在 v2 开的书，先确认这三张表是不是空的。

```text
python3 novel.py doctor
python3 novel.py brief <下一章永久ID>
```

开书初始化检查只对"还没有定稿正文"的项目生效，所以已经开写的项目不会被拦住。这三张表要靠人回头补，机器不会替你发现里面缺了什么。

2. 检查事实记录是不是已经很大。v2 没有封段命令也没有提醒，跑到三四十章的项目很可能已经接近硬上限。`status` 会给出占比。

```text
python3 novel.py seal K0001 K0010
```

### 3.0 项目升级到 3.1 之后要注意的一处行为变化

3.0 的命令会在启动时悄悄把中断的事务恢复掉。3.1 不再这样做。如果你的项目里正好有一笔未完成事务，升级后第一次运行 `status` 或 `doctor` 会看到提示，`commit` 会拒绝执行。这不是升级出了问题——看清楚它涉及哪些文件，再运行 `python3 novel.py recover`。

另外，项目里如果有符号链接，3.1 的 `backup` 会拒绝执行。3.0 在这种情况下会给你一个校验不过、恢复不了的包，所以这是把一个静默失败换成了一条明确的错误。

### 从更早的版本更新

已有 schema 2 的项目直接从当前母版 sync，不需要经过中间版本；没有项目清单或仍是更早结构的项目先用 migrate。

历史章节卡无需批量改写。新章使用现行章节卡；通过 revise 修订旧章时核对已有章卡的 ID 和展示章号，不重跑旧章的全部创作门槛。正式提交必须带本次试算生成的完整批准摘要。

## 3.8 文件头兼容补充

从早期版本迁移时，正文文件保持原字节。带显式迁移记录且永久 ID 在归档基线之前的旧章，可以暂时保留缺省的展示章号、视角和字数；体检会提示。永久 ID、状态及已经填写的其他字段仍需一致，错误不会被兼容规则忽略。后续新章和通过 revise 改过的旧章，必须补齐现行文件头；修改阅读顺序不能把新章变成旧格式豁免章。

## 3.11 → 3.12

结构 schema 仍为 2；不重开书、不重写正文、大纲、设定、研究或已确认决定。旧结构复盘可读；缺少新审查覆盖记录时提示按实际章数补做，不伪造历史完成。旧间隔字段保留兼容，现行日程为三章/十二章。

先 `sync <项目> --check`。已知安装基线与实际共享文件不符时停止，先合并或明确 `--keep-local <路径>`；识别到书内定制插件版本时保留插件包、逐章规则及版本。运行问题记录由各项目后续从可见对话提取，sync 不导入他书日志。已经生成的读取包和候选不被覆盖，继续写作前应重新生成读取包并核对候选。

## 3.13.0 的兼容与采用

沿用 schema 2、原大纲、三章与十二章日程、INIT/REVISE 和摘要批准。同步只更新通用文件，不覆盖正文、风格样本、事实、研究或已填复盘。旧复盘可直接读取，不要求重开书、补填新表或重新选择文风。

新书自动取得样本适用范围的空白提示；旧书若确有文风问题，按已有权限在原样本分析中补充，不强制迁入整份模板。正文阅读用 `outline --reading`，旧质量问题通过 `outline --prompt` 和 `feedback --issue` 接续。

升级改变系统文件，升级前生成的正式读取包、修订摘要需重新核验。升级后单独追加合法运行日志不再使未变的修订摘要失效。未知本地差异仍停止，书内定制插件继续保留。先在副本验证，再按作者授权迁移；具体作品证据和测试结果留在母版外。

3.13 同时纠正两项状态提示。探索目录保留为资料，不当作未提交章节提示删除。文风基线记录正样本、认可名单及认可正文的来源摘要；普通定稿增长不触发重建，撤销认可或修改已认可正文会提示更新。语料不足时明确暂不建立统计基线，旧基线无来源摘要时提示按需核验，不强迫补认章节。

## 3.14.0 的兼容与采用

结构版本保持 schema 2，已初始化的旧项目从新版母版先 sync --check，再按授权同步，不需要 migrate 或重新开书。旧正文、作者设定、风格样本、既有复盘及问题日志保持原字节；新模板和执行规则在后续复盘中生效，不补造历史访谈。旧候选若缺访谈字段可通过 outline --refresh 在原候选接续，保留已有判断。

新轮次的作者访谈与三章/十二章审查合并。实际答复、明确覆盖本轮的已有意见或作者明确跳过均需来源与处理；等待中不记为已交流。正文或规划变化后，先核对已记录答复的适用范围，不强迫重新询问。记录结构验证不证明真人参与，不代替原有创作授权和摘要批准。

更新系统后重新核验已有候选和正式读取包。与原有本地定制发生差异仍先比较处理，不能静默覆盖。母版更新和作品迁移分别实施；具体作品、访谈和测试材料留在母版外。

## 3.15.0 的兼容与恢复边界

结构仍为 schema 2。3.14 的 83 份共享文件基线已加入发布清单，原历史基线保留；当前项目已有的 system_file_hashes 优先用于本地冲突判断。新书和旧书均使用原正式工具，没有新增项目数据格式或第二套审批。

先 sync --check，按明确授权处理各路径。混合文风文件的 keep-local 保留整文件；实际合并时标记缺失、倒置或重复会停止，不能把损坏基线当作空白。本书定制插件继续保留，标准插件登记版按最终安装包核对。

存在未完成事务的项目不能先 sync 绕过阻断。新恢复器按当前项目内事务 ID 定位，兼容旧绝对路径日志；旧日志没有快照哈希时只能验证快照存在、可读及写后一致，无法证明它之前未被改写。缺失或损坏快照保留日志并报错，先保住现场再决定恢复方案。不得删除锁文件、日志或重建空镜像来让检查通过。

author_led 的单项明确代拟、批准理由与 D10 单方向出口沿用原记录。风格硬规则不再要求凑三条；新书样本和分析要求仍保留。正常旧书不重新开书、不补造过去的访谈或审计。定义说明随系统文档更新，已填写的创作文件和历史审计行由项目保留。

升级后重新生成需要的读取包，重新试算候选并核对依据；旧批准摘要不能用于已改变的内容或基线。真实作品迁移须另列范围，系统升级不等于批准正文改稿。
