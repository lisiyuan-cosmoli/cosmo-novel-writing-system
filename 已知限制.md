# 已知限制

## 操作系统

目前仅在 macOS 上完成验证，需要 Python 3.9 或以上。Windows、Linux 与 WSL 等其他环境尚未验证；如需使用，请自行适配并测试，确认文件保存、备份和恢复正常后再用于正式作品。这不是升级操作系统版本就能解决的兼容问题。原生 Windows 无法直接使用当前依赖的 `fcntl` 文件锁；WSL 也尚未完成实测。仓库中配置了 Linux CI，配置存在不代表已有通过记录。欢迎按项目许可适配其他环境，并提交可复现的测试结果。

## 6.0 的边界

- 通用／升级对抗是创作组织方式，不是质量等级。模式由作者在现有 D11 或修订摘要内确认；旧卡缺模式字段继续按旧升级格式解释，不静默改写旧书。
- 提交后的候选存根保留在 `06_归档/_候选存根/`，不进入正常读取、进度、备份或同步。它防止编辑器持有旧文件句柄时的新保存内容被删除；需要长期保存的未提交工作应另行保存，存根不自动获得批准。
- 正常备份在本机事务锁内采集，并核对前后文件集合及内容。外部程序不遵守这把锁；检测到采集变化就停止。此机制不是操作系统快照，不能宣称覆盖任意恶意并发或网络盘一致性。
- 未完成事务的现场备份明确标记混合状态，仍可用于保住现场；不自动修复损坏日志或证明作品内容自洽。
- 文风命令当前候选的核对与正式文风基线分开：导入样本进入 INIT／REVISE，经原有完整摘要批准后才成为正式样本。
- 自测反例证明指定条件得到处理，不提供真实故障概率、全量代码覆盖率或文学质量证明。

## 4.1 题材文风档的边界

- 文风档只给一类书的默认写法，不能替代本书自己的样本。选对档不等于写对声音，仍以作者读感为准
- 表现力指标是粗略计数（情绪词、心想类词、围观反应词），会漏也会误报；它只提醒“往克制漂”，不证明外放得好，也不防止外放过头——过头要靠第二遍语言检查的“反方向”一问和作者判断
- 网文档放行的题材语汇仍可能用滥；滥用由本项目词表和第二遍检查处理
- 旧书改档需要作者批准，已写章节不会因为改档自动改写
- 改口残留（4.1.3、4.1.4）认“动作字＋破折号”“破折号前后同一截重复”“省略号或破折号后接‘不，’”“动作字＋省略号”四种句式；换成逗号的改口、不带“不”字的省略号改口查不到；有意的重复修辞、人物在对白里真的改口（“我……不，是他”）会被误报
- 情绪词重复（4.1.2）只数表现力词表里的那一批词，换成词表之外的同一种套话（比如反复“眼眶一热”）查不到；它和“往克制漂”互相牵制，两条都不证明写得好
- 条款腔（4.1.1）只看【】系统提示与“”对话里的固定词表，查不到叙述里的说明书腔，也查不到词表之外的条款写法；公文、合同原文照录会被误报。它只提醒，是否改由人判断

## 4.0 读者引擎层与三章一批的边界

- 引擎卡、弧卡、悬念账只能证明“读者为什么点下一章”被想过、写下、批准过，不能证明读者真的会点。末句检查由作者判断；上线后以平台完读与追读数据为准，模型复读不算冷读
- 钩子检查只核对悬念账里有这一行、类型合法；使用弧末收束时，若不在弧末须写明自然收束理由。钩子写得是否真是“已经发生的事实”，要读正文才知道
- 钩子检查只作用于作者批准的弧卡范围内的章节。没有弧卡的章节只会被 status、package 与体检提醒，不会被拦
- 引擎卡填好才启用引擎层。旧项目 sync 后补入的是空模板，不会改变旧流程；填不填、何时填由作者决定
- 批次必须在同一会话里连续写完。跨会话接续时，后面几张章卡写于前一章成稿之前，工具只能按时间提醒，判断不了卡的内容是否过时
- 一批里某一章写坏，整批一起返工；三章一批就是为了把这个代价控制住。开篇三章与弧末高潮建议一章一批
- 长输出可能在批尾变弱。每写完一章先存草稿再写下一章，第一遍连读专门检查批尾是否偏离弧卡
- 批次提交仍对每一章单独跑提交闸门；状态快照只要求推进到批末章
- `revise` 可以一次加入多章新正文，但它只跑体检，不跑逐章提交闸门。新章节应走 `commit <批次>`
- 文件名 `08_四遍检查提示词.md` 为兼容同步保留，内容已是两遍检查


## 3.15.2 证据口径的边界

来源不足与明确矛盾的区分仍需要代理阅读判断，软件不自动识别所有越界。必需输入缺失仍停止相应检查；已确认的事实错误不能以“待核对”绕过。两处口径修复尚无真实误改减少或文学提升的量化证据。

分页只输出本次范围，不保存跨命令的实际阅读状态。到达末页、文件摘要一致或已有复盘记录，都不证明全部正文已被阅读；前序覆盖以实际记录为准。局部抽查不替代到期的完整阶段审查。

## 3.15.1 效率精简的边界

同次只读复用不提供文件系统事务快照，也不是跨命令持久缓存；并发外部编辑仍需重新取材，正式写入继续在锁内核对。分批阅读全文仍需每次检查正文版本，长事务历史和网络盘的性能尚未专门验证。

读取包仍保留完整必要控制材料；旧事实的共同说明虽已合并，逐章预览和长期保留项仍会增长。状态页显示原始事实存量，正式包按实际内容提示预算压力；封段是否值得做仍需结合内容判断，不自动生成摘要。

十二章外部研究按现行规则可选；不开展要说明理由，复用材料要核对其范围与当前问题是否相符，不能把旧记录充作本轮已开展。完整影子验证与写入前后复核保留。日常接续的局部阅读不能充作全书审读，软件提速及合成测试不证明文学质量或作者实际等待时间改善。

原子替换保留已有普通文件的 POSIX 权限；新文件默认仍为私有权限，显式 mode 优先。本版不新增 ACL、扩展属性或所有者迁移承诺，也不自动修复旧项目已经丢失的权限。

## 机器能做什么、不能做什么

- 机器检查擅长永久 ID、文件存在、状态新鲜度和跨表引用，无法可靠判断人物动机、语言质量和阅读吸引力
- 候选 SHA-256 可以证明用户批准后文件没有被替换，不能脱离宿主环境证明批准者身份
- 流程审计可以验证字段、状态与净字数，不能证明某个人确实认真执行了阅读和检查
- 章节卡的重复动作清单会检查检索命令的形态与取样覆盖，不能证明命令真的跑过
- 插件可以声明章节可写文件并保护项目数据，但是否触发题材规则仍由章节卡明确记录；通用基座不靠情节关键词猜测

## 插件升级

- 插件文件分母版托管（逐章检查）与用户拥有（设定、运行表、归档）。`sync` 只更新前者，后者一个字节都不动
- 托管文件存在本地修改或缺少可核对的安装基线时，sync 先报告冲突；明确 --keep-local 则保留，明确 --replace-local 才归档旧内容并替换。未知来源不能自动视为母版版本
- 本书定制包与用户文件保持原样；标准包更新后，登记版本按最终包核对。单个校验器相同不代表插件版本相同
- `verify` 核对结构与版本，不能证明插件内容适合这本书

## 题材插件

- 插件分两层：逐章检查进读取包（每份不超过 1200 字符，母版检查强制），题材手册不进包、用 `novel.py guide` 调阅
- **手册不进包意味着 AI 不会自动读它。** 需要题材判断依据时要主动调阅，检查文件末尾写了路径
- 插件数字与句式建议都属于编辑提醒，可按作品调整。事实、世界规则和知情范围仍应自洽；作者明确选定的风格硬规则保存在本书样本文件中
- 插件的逐章检查只能核对"这一章要不要停下来看一眼"，不能判断题材规则用得好不好

## 开书闸门

- 它证明的是：十一项基础决定被逐条提出过、有明确来源、在批准时全部已确认，并且用户看到的清单在写入前没有被换掉
- 它**不能**证明批准者的现实身份，也不能证明用户真的读过每个候选。有人故意把 AI 代拟填成用户原案，工具只能核对记录自洽，不能核对事实
- `guided` 模式要求关键决定留下两个以上候选，能挡住"只给一个候选让人点头"，挡不住"给两个明显凑数的候选"
- 从旧版升级的项目标为 `legacy`，可以继续写。**系统绝不把旧项目静默标成 confirmed**——那等于替用户签字
- 判断旧项目是否已经进入创作阶段时，初稿与待返工同样算数，不是只看已定稿
- 暂定书名与结构路线在开书之后仍可修改，但要走独立维护事务，不能混进章节候选

## 待确认与事实边界

- 「待确认」按**字面标记**判定（待用户确认、待定、待改选……）。它挡得住"写着待确认还打勾定稿"，挡不住有人把标记删掉再定稿
- 「正文事实」行按**推断词**判定（推想、推断、坐实、似乎……）。它挡得住把判断写进事实行，挡不住换一种说法把判断写成陈述句
- 世界规则冲突只能做**字面提醒**：正文里出现了规则中引号括起来的说法、而章卡没声明时提示。**规则被违反时可能没有字面命中**。合成例子：规则写“信标只能传递颜色”，正文却让接收者听见远处说出的一整段话；违规情节没有逐字复用规则的关键说法。这一类仍要由人逐条核对，放在第一遍第 8 条。
- 系统不判断推测是否应该升级为事实，只保证它不会因为被反复引用而自动变成事实

## 结构与格式

- 核心以 Markdown 文件保存创作内容，复杂表格仍需要遵守现有列格式
- 状态快照 A–E 各节的**第一行按表头处理**。表头可以改名，但不能删掉
- 伏笔与线索编号写成 `F` 或 `C` 加两位以上数字，`F07`、`F100`、`C205` 都可以，位数没有上限；不要在编号和数字之间加空格
- 时间地点允许自然语言。只有使用精确公历日期的项目才能生成按日排序的时间图

## 写入与回退

- 章节提交只允许写当前章与运行账本。旧章和全局设定用 `revise` 独立候选事务提交。普通编辑不形成事务，不能靠 `rollback` 撤销；备份须恢复到新目录比对。修订体检核对全书结构，不执行要求快照推进到某个旧章的逐章闸门，不证明语义一致
- `rollback` 撤销最近一笔提交，而它自己也是一笔提交。**连按两次是重做，不是再退一章**。要跨多笔回退，逐笔确认，或从备份恢复到新目录
- 多文件提交提供崩溃恢复，但**恢复不会自己发生**。进程被强制终止后，只读命令照常报告"存在未完成的事务"，写入命令直接拒绝，直到你看过现状并显式运行 `novel.py recover`。恢复和提交一样是一次写入，不能由一条 `status` 顺手触发
- 恢复备份只能写入新目录，避免覆盖现有项目
- 备份不含 `.novel` 事务历史。正常完整备份恢复并核对后可继续写，`history` 与 `rollback` 从新目录重新开始；中断或损坏事务的现场备份可能包含混合状态，只用于保全现场，不能据此保证可继续写，也不能替代原事务恢复资料
- 事务互斥由操作系统的 `flock` 持有，可靠范围是**同一台机器上的本地文件系统**。多人协作仍需外部版本控制
- 检查与写入在同一把锁内完成：批准摘要在锁内重算，期间有人改了同一批文件就整笔拒绝。这挡的是"旧批准覆盖新提交"，挡不住两个人各写各的章然后内容打架
- `_备份`、`_读取包` 这类输出目录也逐级检查符号链接。想把备份放到项目外，目前只能自己复制出去，工具不提供外部输出参数
- **不建议把项目放在 iCloud Drive、Dropbox、OneDrive 或网络盘里**。这些位置上 `flock` 行为不保证，同步客户端也可能在事务中途搅动 `.novel/`。取不到文件锁时工具会明确报错并指出这一点，不会假装加上了锁
- 只读命令（`status`、`doctor`、`history`，以及不带 `--write` / `--apply` 的 `brief`、`package`、`commit`、`seal`）在任何异常状态下都不改动项目里的任何字节。中断后的恢复只由显式 `recover` 触发
- 备份对项目有效内容里的符号链接采取默认拒绝：文件链接、目录链接、断裂链接、指向项目内外的链接一律拒绝，并指出具体路径。不静默解引用，也不生成随后无法校验的包

## 公平推理核对（仅悬疑插件）

- 机器核的是**章号先后与登记有无**：揭晓章之前有没有首现章、那一章在不在正文里、首现那一章的事实记录里登没登记过它
- 机器核不了**读者推不推得出来**。线索铺在纸上但写得太隐、或者铺了却没给读者任何理解它的钩子，机器一律看不出来。那是 08 第二遍和插件检查第 7 条的事
- 它依赖你如实填写「首次出现章」。填错章号能骗过它——但那一栏填错，提醒会告诉你那一章的事实记录里没有这条线索

## 多章规划

- 4.0 起规划与生成都能按批：一批的章数不超过 `batch_max`（缺省 3，最多 5）。批内章节要在同一会话里连续写；跨会话接续时，后面几张章卡要按已提交状态重新核对，工具只能提醒，不能判断卡的内容是否过时
- 一次最多规划五章
- 工具按文件修改时间提醒"这张卡写于最近一次提交之前"。它判断不了卡的内容是否真的过时，只能告诉你时间上存在这个可能

## 带宽

- 读取包保留阶段摘要、最近若干章完整条目、较早条目的截取预览和显式标记的长期保留项。旧条目不再全文进入包；需要细节时调阅原条目或在章节卡声明旧章。定期封段仍可减少历史预览的累计体积。
- `status` 与 `package` 在事实记录超过目标线三成、或成品包到达硬上限八成五时提醒。提醒是提醒，封段仍要人来决定摘要内容
- 封段要求阶段摘要**先写好**。工具只做机械搬运，不代写摘要
- 章节卡声明整章调阅（不给片段锚点）是读取包变大的头号原因

## 目标字数

- 字数目标是**可选的**。写「不设」即可，`status` 会只报事实不报偏离
- 章数仍然要填：它决定结构。字数是结果，不是目标
- 设了目标时，预测只在已定稿 ≥3 章之后开始，且**不会替你补字数**——提醒里给的是改目标、加章数或接受较短成书

## 文风的来源

- 正样本是这本书文风的**唯一独立参照**，必须先于正文存在。让模型先写第一章、再拿它当正样本，等于让被衡量的对象自己定标准；这会使样本失去独立参照作用
- 开书要求正样本 ≥800 字、分析 ≥5 项；硬规则按实际约定填写，不设数量下限。已经开写的项目沿用旧的 500 字门槛，不回溯拦人
- 工具只出提示词、核对够不够用、把选定的样本写进去。**它不生成文字，也不替你选**
- 候选是否具有实际表达差异仍需阅读。机器只核对候选记录、来源与理由；通常保留比较候选，作者已有明确方向且明确委托单项代拟时允许一段，不能据此宣称文学差异或作者阅读已被验证

## 文风度量

- **已定稿不等于文风被认可。** 基线只用正样本加用户在 `02_风格样本.md` 明确列出的章节。模型写的正文默认不进基线——否则第一章的模型腔会变成后面所有章的标准
- 统计偏离只做解释，不做目标。报告与改写提示词都不会要求"朝基线方向调整"

- 只测**可计算**的部分：套话命中、句长与段长分布、比喻与连接词密度、感官分布、成对结构。叙述者替人物下结论、人物声音是否可分、情绪有没有被动作承担，机器看不见，留给逐章第二遍语言检查
- 基线来自本书自己的正样本与**用户明确认可的**已定稿章节，**不是通用标准**。合并语料不足 2000 字时不出分布层基线，只报套话命中——统计量在那个体量上不稳定，给数字比不给更糟
- 已定稿章不足 3 章时偏离按百分比报告，够 3 章后自动换成逐章标准差倍数
- **不打分、不拦提交**。这些指标全都能刷：把长句拆开塞几个短句，数字立刻好看，文本更碎
- 母版 `AI腔词表.txt` 只收跨模型稳定的公共核。各家模型还有自己的尾巴，而且模型会更新——那部分靠 `style --suspect` 从你的实际稿件里发现，积累进 `AI腔词表_本项目.txt`
- 词表必然有误报。网文题材会天然命中一部分动作与氛围条目，那是题材语汇不是缺陷，用 `允许AI腔.txt` 豁免并写明理由
- 工具只定位，不自动改写。让同一个模型去改自己的 AI 腔，结果通常是换一套套语

## 环境

- 当前没有图形界面，需要 Python 3.9 或更高版本
- 统一命令以稳定退出码和人类可读输出为主，没有为每条命令提供通用 `--json` 输出
- 故障注入开关（`NOVEL_SELFTEST_FAULTS` 等）只在自测显式打开时生效，正常使用中环境变量不会影响提交

## 3.8 补充边界

- 关联核对按文件职责与展示顺序保守列范围，不会自动理解人物秘密、事件因果或文本是否等义。后续正文按组核对，长篇仍需要人工判断实际影响。
- 关联核对能防止省略对象、复用过期结论和替换批准内容，不能证明填写者已经认真阅读。写“已核对”且给出理由仍是操作者的陈述。
- 通过 revise 的正文内容、设定和运行账本修改才触发上述关联核对；普通编辑不会自动产生核对记录。既有文件不会在 sync 时自动补头或修正正文。
- 只改正文文件头不触发故事关联核对，但身份、视角、章号、字数和状态必须通过共同校验。改书名等元数据不增加故事核对。
- 显式迁移项目在归档基线之前的旧永久 ID 可保留缺省的展示章号、视角和字数，体检提示而不改原文件；已有字段错误仍阻断。新章与修订后的正文无此兼容例外。
- 探索稿使用已有临时候选区，既不注入正文读取包，也不进入系统备份。采纳时仍需按正式流程核对；没有自动将探索稿转为定稿的工具。
- 未进行真实读者盲测和长期收益对照。新加的阅读与流程取舍记录是执行方法，不是已经取得的效果证据。

## 3.9 写作质量试验边界

- 诊断、定点改稿与匿名比较是可执行的方法，尚无新书长期实写收益证据，不能宣称必然提高文学质量。
- 同一生成者共享上下文、读者先后顺序和前情摘要均可能影响判断。匿名不能消除这些影响，模型扮演读者不算真实反馈。
- 不自动生成匿名阅读包、分发稿件或收集反馈；模板需复制后按本轮实际内容填写。无读者时可以继续正常写作。
- 完整手册不默认入包，但两遍检查中的短版提示会略增加读取量，以实际包的带宽检查为准。
- sync 更新系统手册和空白模板，不覆盖既有读者记录。误把作品资料填进同步模板原件会有覆盖风险，须按说明写在副本中。


## 3.10 开书探索与文风推荐

开书时先提供已有研究报告、故事方案、参考作品和试写；没有材料再按需研究。已有方案或明确文风可以沿用，不新增研究门槛。`discover --prepare` 准备可编辑记录，`discover --import <文件>` 保存原始材料副本，`discover --prompt` 给出研究任务。`voice --recommend` 给出文风推荐任务，两者本身都不联网或生成推荐结论。

需长期保留时使用 `discover --stage`，研究记录与原始材料进入现有 foundation 或 revise 候选；按原有完整摘要批准后才正式归档并进入备份。研究报告、文风推荐及材料清单不整份进入逐章读取包，只有作者采纳的简短约定进入创作意图与风格样本。方法与空白模板可以升级，填写后的记录和原始材料不被同步覆盖。

具体方法见 `02_检查层/15_开书探索与文风推荐.md`。导入只能证明材料保存成功，不能证明已经阅读、内容可信或作者已采纳。推荐可能不合适，研究可能耗时；可以否决、暂缓、缩小范围或跳过，不能保证提升文学质量或收入。

## 3.11 结构论证与滚动复盘

outline 提供任务、候选准备和资料变动提醒，不自动分析故事或给文学质量合格证。复盘完成状态由记录自述，输入更多推理或填写更多表格都不能证明结论可靠。提醒仅比较指定核心文件、大纲非章节结构区、未写章节安排和此前定稿正文，不覆盖所有插件设定、外部资料或语义依赖。

3.11 当时采用五章节奏；当前已由三章小审查、十二章大审查承接，阶段结束或重要偏离可提前，不叠加旧五章检查；提醒不成为章节阻断。开书阶段仍须认真论证已有路线，但旧项目不因缺少新记录退回开书。新书的大纲有可选阶段规划区；sync 保留旧大纲，需要时在原文件手动加入。

长期反复推翻大纲会增加返工。每轮只处理重要问题，保留有效内容，暂缓注明触发条件。复盘资料版本刷新会把自述状态恢复待复盘，不能以刷新代替回读。候选记录未正式归档前不在普通系统备份中。

## 3.12 阶段审查与运行日志

三章/十二章是当前默认检查节奏，尚无长期读者数据证明其最优。到期提醒不会替执行代理完成阅读和榜单研究，也不以机器评分阻断创作；代理仍须在下一章设计前执行或明确记录受限和暂缓。

运行问题由代理从已可见证据提取，工具不自动抓取其他任务、完整聊天或后台运行；摘录完整性、原因判断和验证结论仍需人工审阅。问题日志采用原子单文件追加，不改变创作事务 HEAD；它不是创作授权。

共享文件只在有安装基线或已知上一发布版本时能可靠识别本地修改。更老项目缺乏可信基线时，存在的差异按冲突停止，升级前需人工比较并逐项选择保留或替换。带书内版本标记的定制插件会保留；其他未登记的定制按差异冲突处理，需手工核对。科幻奇幻的附加内容校验仍按插件清单显式启用，核心不会据此证明世界规则在语义上成立。

## 3.13 连续阅读与纠错的范围

正文分页、未解决问题提示与更具体的审查要求可减少遗漏，不能证明审查者独立、读者会追读或小说质量已经提高。小动作、短段落、整齐结尾和词频均不能用于计算 AI 作者概率或商业成功率。

同一模型仍可能维护自己先前的判断。读完干净正文再看旧评价只能减少部分先入之见，不能称为独立盲测。真实效果需结合具体改稿、作者或目标读者的阅读反馈判断；没有反馈可继续创作并保留未知，不虚构验证。

日志摘要可发现未经同步更新摘要的损坏，不是签名、身份认证或防篡改历史。revise 对日志的分离仅避免无关记录导致重复审批，不放宽任何创作文件或候选的哈希绑定。

## 3.14 作者访谈与审读范围

作者访谈由执行代理在实际对话中进行；outline --interview 只列任务和候选记录状态，不主动联系作者，不后台监控章节，也不能验证回答者身份。字段非空或状态已交流仍是操作者自述。沉默不等于回答，资料刷新不等于读过，旧记录兼容不等于补做访谈。

等待回复可能增加阶段耗时；已有本轮答复应直接接续，十二章不重复提问，作者明确跳过时记录继续范围。检查文学质量仍有主观性与漏判风险；加强审查不保证追读或收入提升。外部审读的历史 JSON 与 Markdown 可能存在不同阶段结论，需核对来源和纠正记录，工具不会自动理解其语义。

## 3.15 可靠性修复的边界

- 恢复依当前项目定位事务并验证输入及结果，缺快照保持待恢复。新事务记录 before 哈希；旧日志没有该字段时，工具无法证明此前的快照未被改写。
- 事务、候选与恢复路径拒绝预置符号链接。检查不能证明任意恶意本地进程与文件操作之间完全无竞态，也未覆盖所有云盘和网络盘锁语义。
- 备份恢复显式限定普通文件与唯一清单路径。外部工具制作的目录成员、特殊设备或链接不属于正式备份格式，恢复会拒绝；不能用跳过校验来导入。
- 合成回归和长篇规模查询不证明真实作者负担降低、文学质量提高或市场效果。三章/十二章节奏继续在真实使用中观察，不为本次修复增加访谈或填表。
