# 项目配置

> **读取包目标：58000**　**读取包硬上限：62000**　**计量口径：成品包字符**
>
> 新项目应根据模型上下文、作品规模和近期章节长度实测调整。修改时写明理由。
>
> 机器配置以 `project.json` 为准。本文件供作者和协作者阅读，保存本书的工作约定。

---

## 一、本项目基本信息

| 项 | 内容 |
|---|---|
| 书名 | |
| 题材 | |
| 目标字数 / 章数（字数可写「不设」） | |
| 当前进度（永久 ID，四位数 K0001） | |
| 单章工作模式（见第五节） | |

## 二、已启用插件

插件通过 `_工具/novel.py plugin` 安装和核验。插件不得改写核心模板，只能在自己的设定、运行、检查和归档目录内增加文件。

| 插件 | 用途 | 状态 |
|---|---|---|
| | | |

> `project.json` 中的 `plugin_decision` 与 `enabled_plugins` 才是机器判定依据。不开插件也是完整配置，需明确记录为 `none`。

## 三、每章生成时读取的文件

核心文件由读取包工具按预算组装。

- [x] `00_设定层/01_固定设定.md`
- [x] `00_设定层/02_风格样本.md`
- [x] `01_运行层/04_状态快照.md`
- [x] `01_运行层/05_伏笔表.md`
- [x] `01_运行层/06_事实记录.md`
- [x] 最近一至两章正文
- [x] 本章章节卡

已启用插件的读取文件由各插件的 `plugin.json` 声明，读取包工具自动纳入。不要把插件安装说明复制进上下文。

## 四、每章确认时更新的文件

核心提交至少覆盖以下内容。

- [x] `05_正文/K0001.md` 这类正文文件
- [x] `01_运行层/04_状态快照.md`
- [x] `01_运行层/05_伏笔表.md`
- [x] `01_运行层/06_事实记录.md`
- [x] `00_设定层/03_分章大纲.md`
- [x] `06_归档/章节卡_K0001.md` 这类章节卡
- [x] `06_归档/流程审计.md`

启用插件后，插件声明的运行表与归档表也必须进入同一笔事务。所有文件一起通过检查才正式落盘。

## 五、单章工作模式

完整模式用于结构方向改变、不可逆事实出现、关系被重新定义、世界规则边界改变、重要承诺兑现等高影响章节。

过渡模式用于日常行动、余波处理、信息补充、氛围建立、位置转换等低影响章节。它仍要检查因果、知情范围、连续性、复查和提交包。

| 项 | 完整模式 | 过渡模式 |
|---|---|---|
| 章节卡 | 全填，两栏 🔒 由用户提供 | 两栏 🔒 由用户提供，其余可协作拟定 |
| 梗概 | 需要比较时给三个走向；已有明确依据可沿用一个 | 需要比较时给主走向与替代走向；已有明确依据可沿用一个 |
| 诊断 | 两遍检查完整执行 | 追读与连续性完整执行，语言快查 |
| 修改与复查 | 必做 | 必做 |
| 事务提交 | 必做 | 必做 |

模式没有固定比例。若普通章节长期难以完成，应先缩短步骤，再考虑增加检查项。

4.0 起一批默认三章，在同一会话里连续写、一次提交。一批的章数写在 `project.json` 的 `batch_max`（1 到 5，缺省为 3）。第 1—3 章、弧末高潮章和作者指定的关键章逐章写。

## 六、全书改稿阶段的插件检查

核心改稿流程保持题材中立。插件的全书检查从插件检查文件读取，结果写入插件自己的归档文件。

| 项 | 内容 |
|---|---|
| 本书需执行的插件检查 | 由已启用插件自动汇总 |
| 改稿后需重建的插件运行表 | |

## 七、流程审计与封段

每章的模式、步骤状态、跳过理由、耗时与问题来源统一写入 `06_归档/流程审计.md`。本文件不重复保存逐章数据。

事实记录每十章左右封一次段。封段前先在 `01_运行层/06_事实记录.md` 写好阶段摘要，再运行 `python3 novel.py seal <起> <止> --apply`。`status` 与 `package` 会在事实记录偏大时提醒。

| 项 | 内容 |
|---|---|
| 已封段范围 | |
| 下次计划封段 | |

## 八、当前仍未了结的事

> `🔴阻塞` 会阻止正式读取包和提交。`🅿️提示` 只提醒。`✅` 表示已经解除。母版只保留表头。

| # | 事项 | 阻塞 | 状态 |
|---|---|---|---|
